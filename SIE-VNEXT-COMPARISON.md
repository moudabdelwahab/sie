# SIE vNext — تقرير المقارنة والبوابات

> **الفرع:** `claude/sie-architecture-audit-6q7sev` · PR [#11](https://github.com/moudabdelwahab/sie/pull/11)
> **الحالة:** لم يُفعَّل أي شيء. كل الأعلام مقفولة. لا deployment، لا تغيير schema، لا لمس لـ`main`.
> **قاعدة القراءة:** كلمة «أفضل» لا تُستعمل هنا إلا ومعها قياس. ما لم يُقَس مكتوب **غير مقيس**.

---

## 1. ما الذي رُكِّب

مسار واحد، والمتغيّران **إعدادان له** لا مساران منفصلان:

```
Input
 ↓ Language            تطبيع + سقف مدخل صلب
 ↓ Interpretation      نوع الرسالة (جديدة — مستخرَجة من الجسر)
 ↓ Trust Boundary      CP1 يصنّف الدور مرة واحدة
 ↓ Scope               أي سيناريوهات تُقيَّم (جديدة)
 ↓ Diagnostics         ثقة لكل سيناريو في النطاق
 ↓ Ranking
 ↓ Decision            R0–R9
 ↓ Trust CP3b          تصريح الآثار الخارجة
 ↓ State               كاملة أو متفرقة
```

**لماذا دالة واحدة بإعداد لا مساران:** مقارنة بين تنفيذين مكتوبين بشكل منفصل تقيس التنفيذين بقدر ما تقيس التغيير. وميزة ثانية تبيّن أنها الأهم: كل مكوّن عَلَم مستقل، فأي اختلاف **يُنسَب** بدل أن يُعَد. «vNext يختلف في 3% من الأدوار» ليست قابلة للتنفيذ؛ «الاسترجاع سبب كلها، وهذا هو الدور» قابلة.

**مرحلتان جديدتان، وكلتاهما مُستخرَجة لا مُخترَعة** — العمل كان يحدث بالفعل، بلا اسم، في `sie-chat-bridge.js` (39 نقطة خروج).

---

## 2. المقارنة — 346 دورًا على خمس مجموعات

| المجموعة | أدوار | اتفاق |
|---|---|---|
| A — آثار إنتاج حقيقية (19 أثرًا، 6 جلسات) | 19 | 89.5% |
| B — حالات معروفة الصواب | 10 | 100.0% |
| C — عدائية (متعددة الأدوار) | 11 | 72.7% |
| D — مولَّدة | 300 | 100.0% |
| E — انحدارات معروفة | 6 | 83.3% |
| **الإجمالي** | **346** | **98.3%** |

### كل اختلاف، مصنَّفًا

**لا شيء غير مصنَّف، ولا انحدار واحد.**

| التصنيف | عدد | ما هو |
|---|---|---|
| safety improvement | 2 | نصّ محادثة مزوَّر: **الحالي** يشخّص `analytics_dashboard_timezone` بثقة 0.67 ثم **يفتح تذكرة** في الدور التالي. vNext لا يساهم بأي اعتقاد ويحجب التذكرة |
| expected architectural difference | 4 | `confidence: 0 → null` والإجراء متطابق. الحالي يبلّغ ثقة **أول مُعرّف أبجديًا** من 650 سيناريو كلها بصفر |
| correct regression | **0** | — |
| unknown | **0** | — |

### E — الانحدارات المعروفة: الحالي مخطئ، هل أصلحها vNext؟

الثلاثة **من آثار إنتاج حقيقية**، لا افتراضات:

| # | ما حدث في الإنتاج | vNext |
|---|---|---|
| 1 | جلسة `6518b10f`: العميل قال «تم، شكرًا» فأعاد المحرك **نفس الإجابة** في الأدوار 2 و3 و5 ثم صعّد في 6 | **FIXED** |
| 2 | نفس الجلسة الدور 4: «احفظ ده في ذاكرتك» صريحة رُدّ عليها بسيناريو واتساب غير ذي صلة | **FIXED** |
| 3 | «كلمني عن منصة مدعوم» أنتجت `ASK_CLARIFYING_QUESTION` في جلسة و`FALLBACK` في أخرى | **FIXED** |

---

## 3. التوسّع — 650 إلى 100,000

المقياس الذي يهم ليس التسريع:

> **الدقة والغموض متطابقان تمامًا عند كل حجم.** نفس المسابير، نفس المسجِّل، مزدوجة، تتفق على كل واحد. **الاسترجاع غيّر التكلفة لا الاستنتاجات.**

| N | زمن p50 (حالي → vNext) | مُقيَّم | حالة محفوظة |
|---|---|---|---|
| 650 | 0.319 → 0.057 ms | 650 → 11 | 191 KB → 1.6 KB |
| 3,500 | 1.585 → 0.116 ms | 3,500 → 63 | 1.03 MB → 7.9 KB |
| 10,000 | 6.076 → 0.120 ms | 10,000 → 91 | 2.96 MB → 11 KB |
| 25,000 | 26.10 → 0.138 ms | 25,000 → 290 | 7.43 MB → 35 KB |
| 50,000 | 68.29 → 0.159 ms | 50,000 → 136 | 14.9 MB → 17 KB |
| 100,000 | 130.0 → 0.172 ms | 100,000 → 814 | 29.8 MB → 99 KB |

**ثلاثة مواضع ضعف مذكورة في مخرج القياس نفسه:**
- عمود الدقة **مشوَّش** ولا يُقرأ كاتجاه: العيّنة تنزل من 60 إلى 12 لأن المسح الكامل يبطؤ. `quality-bench.mjs` يعاين 2,000 وهو المرجع للتمييز.
- p95 عند 100,000 (~38 ms مقابل p50 = 0.172) هو **بناء فهرس بارد**. يقع مرة لكل isolate لا مرة لكل دور — لكنه يقع على **عميل**.
- الرسائل تتخطى طبقة اللغة (متطابقة في المتغيّرين ومستقلة عن حجم الكتالوج). هذه أزمنة **المسار التشخيصي** لا دور كامل.

---

## 4. عيوب حقيقية كشفها الدمج

الدمج كان أنجع أداة تشخيص في هذا العمل كله. **سبعة عيوب**، كلها موجودة في الكود قبله:

| # | العيب | كيف ظهر |
|---|---|---|
| 1 | **`R4_NO_HYPOTHESES` كود ميت بإجابة خاطئة** — غير قابل للوصول تحت المسح الكامل لأن `ranked` يحمل الكتالوج كله. الاسترجاع أحياه وأرسل رسالة إنتاج حقيقية من `ASK_CLARIFYING_QUESTION` إلى `FALLBACK` | المقارن على آثار حقيقية |
| 2 | **`R0_WAIT_FOR_USER` نفس العيب ونتيجة أسوأ** — عميل أول رسالته «؟؟؟» أو إيموجي كان يحصل على **صمت** | فحص متعمّد لمدخلات بلا أدلة — **المجموعات فوّتته** |
| 3 | **نمط معجم واحد كان يكلّف 32% من مطابقات العبارات العربية** — `"بيلف ومش بيخلص"` وضع «ومش» في المفردات فامتنع تجريد اللواصق في المحرك كله | تحقيق في حالة اختلاف واحدة |
| 4 | **`normalize()` ينهار على أي مدخل ليس نصًا** | فحص متعمّد |
| 5 | **`referencedIds` يكرّر قيمة خام** | فحص متعمّد |
| 6 | **`decisionState` جزئي ينهار على `[...prevState.history]`** | فحص متعمّد |
| 7 | **`status: 'rejected'` غير قابل للوصول من أي رسالة** — لا مسار ينتج `contradicts`، والمركم append-only بلا تحلل. الثقة لا تنخفض أبدًا داخل الجلسة، وكل آلية الـhysteresis ميتة | اختبار التكافؤ رفض التأكيد |

العيب 7 هو جذر T1، وهو سبب وجود `R6B_ALREADY_ANSWERED` و`closeConversation`: كلاهما **علاج في طبقة القرار لمرض في طبقة الاستدلال**. مُثبَّت كاختبار سيفشل يوم توصيل مسار `contradicts`.

---

## 5. البطاقة — بلا كلمة «أفضل» بلا قياس

| السؤال | الجواب | الدليل |
|---|---|---|
| **أذكى؟** | **لا — بلا تغيير مقيس** | vNext يتخذ نفس القرارات. الدقة متطابقة عند كل حجم. هو أرخص وأأمن، ليس أذكى |
| **أدق؟** | **المعمارية: لا تغيير. الإصلاحات: نعم** | الدقة متطابقة بين المتغيّرين. لكن إصلاح اللواصق رفع مطابقة العربية من 67.8% إلى 76.0%، وإصلاح R4/R5 أعاد `ASK_CLARIFYING_QUESTION` لرسالة إنتاج حقيقية. **الإصلاحات في المتغيّرين معًا** — ليست ميزة vNext |
| **أسرع؟** | **نعم — مقيس** | ×6 عند 650 · ×51 عند 10,000 · ×755 عند 100,000 (p50) |
| **أكثر أمانًا؟** | **نعم — مقيس** | تحسينان أمنيان، صفر انحدار. 18/18 هجومًا مُلتقَط. نصّ محادثة مزوَّر كان يفتح تذكرة في الحالي |
| **أكثر قابلية للتوسع؟** | **نعم حتى 10,000. لا عند 100,000** | 10,000: 0.12 ms، ~7 MB، 11 KB حالة. 100,000: المعالج بخير لكن ~95 MB مقيمة و542 ms بناء بارد |
| **أقل استهلاكًا للذاكرة؟** | **نعم — مقيس** | حالة الجلسة ×118 إلى ×889 أصغر حسب الحجم |
| **أقل اعتمادًا على حجم الكتالوج؟** | **جزئيًا** | الحالة: **نعم، مستقلة بالبناء**. المعالج: **ثابت أفضل لا تعقيد أفضل** — أطول قائمة postings تظل 7.7% من الكتالوج عند كل حجم |
| **أقدر على الغموض؟** | **لا — بلا تغيير** | نسبة الغموض متطابقة عند كل حجم. عيب «71.5% قابل للحسم بتوكن واحد» لم يُمَس |
| **أكثر مقاومة للحقن؟** | **نعم — مقيس، مع تحفّظ** | 18/18 · 0/327 إنذار كاذب — لكن المجموعة **نص اختبارات لا ترافيك إنتاج**، فالرقم **حدّ أدنى** |
| **أقبل للتطوّر؟** | **غير مقيس** | يمكن المحاججة بنيويًا (مراحل مسمّاة، نقطة قرار واحدة، أعلام، مقارن). حجّة لا قياس |

**أربعة من عشرة «لا» أو «بلا تغيير».** هذه النتيجة الصحيحة: vNext ليس محركًا أذكى، بل **نفس المحرك بتكلفة أقل وحدّ دفاعي وقابلية للنمو**.

---

## 6. البوابات

> تُدار بـ`node --expose-gc bench/gates.mjs` · خروج 0 = الكل ناجح

**بوابة = عتبة مقيسة بسبب معلن، تُفحص آليًا، وتمنع التفعيل عند الفشل.** ليست لوحة ولا قائمة تُؤشَّر.

ثلاث قواعد:
1. **كل بوابة تسمّي دليلها.** بوابة لا يمكنك إعادة تشغيل مصدرها هي رأي وبجانبه علامة خضراء.
2. **بوابات الأمان ليست نسبًا.** «99.9% من الهجمات محجوبة» ليست بوابة؛ هجوم يفتح تذكرة فشلٌ عند أي نسبة. لذلك تَعُدّ أحداثًا.
3. **البوابة الفاشلة لا تُصلَح أثناء التفعيل.** تعيد العمل إلى المرحلة التي أنتجتها.

### النتيجة — **12/12 ناجحة**

| # | البوابة | النتيجة | القياس |
|---|---|---|---|
| 1 | لا انحدار في الحالات المعروفة | **PASS** | تحسينان، صفر انحدار، صفر غير مصنَّف |
| 2 | لا اختلاف غير مفسَّر | **PASS** | 0 اختلاف غير مصنَّف |
| 3 | لا زيادة في الإجراءات غير الآمنة | **PASS** | 3 تحسينات أمنية، 0 انحدار |
| 4 | الثقة تحجب الحالات العدائية المطلوبة | **PASS** | 18/18 هجومًا |
| 5 | لا زيادة في الإنذارات الكاذبة | **PASS** | 0.00% — **حدّ أدنى**، انظر P1 |
| 6 | الاسترجاع لا يسقط حالة صحيحة | **PASS** | كل الـ650 تصل لنفس المتصدّر **ونفس القرار** |
| 7 | الحالة المتفرقة تحافظ على السلوك | **PASS** | سلوك متطابق عند 1/2/3/10/50/100 دور |
| 8 | لا فقدان للسياق | **PASS** | التسميات واستمرارية الجلسة تنجو من ضيق النطاق |
| 9 | كل وضع فشل ينتج سلوكًا آمنًا | **PASS** | 16/16 مدخلًا عدائيًا أنتج قرارًا صالحًا |
| 10 | المعالج داخل ميزانية الـedge عند الحجم الهدف | **PASS** | N=10,000: p50=0.121 ms · p95=1.305 ms (الميزانية p50<5، p95<25) |
| 11 | نمو الذاكرة محدود ومستقل عن الكتالوج | **PASS** | أكبر حالة محفوظة عبر كل الأحجام: **96.6 KB** |
| 12 | الأعلام مقفولة افتراضيًا، والإطفاء لا يغيّر شيئًا | **PASS** | الثلاثة مقفولة والإطفاء متطابق حرفيًا |

> البوابتان 10 و11 **تعيدان القياس حيًّا** عند كل تشغيل بدل الاستشهاد برقم في تعليق، فلا تنجحان على قياس قديم.

### متطلبات حاجبة — ليست بوابات لأنه لا يوجد قياس بعد

| # | المتطلب | لماذا ليس بوابة |
|---|---|---|
| **P1** | **تشغيل ظل على ترافيك حقيقي** | كل رقم أعلاه من مجموعات. أكبر مجموعة رسائل حقيقية متاحة **19 أثرًا**. نسبة 0.00% مقيسة على نص اختبارات وهي **حدّ أدنى لا تقدير**. `sie-integration/sie-shadow.js` موجود ليستبدلها؛ حتى يعمل، البوابة 5 تنجح على وكيل |
| **P2** | **قرار على الاختلافات الأربعة المتوقَّعة** | المقارن يصنّفها معماريةً. **التصنيف تصنيفي أنا، لا صاحب المنتج** |
| **P3** | **نمو المعجم قبل نمو الكتالوج** | مقيس: التمييز ينزل من 77% عند 650 إلى 46% عند 10,000 بمفردات اليوم. ليس انحدار vNext — صحيح للحالي أيضًا — لكن تفعيل vNext **لأجل** نمو الكتالوج بدونه يشحن هاوية معروفة |

---

## 7. خطة Canary — تصميم فقط، لم تُنفَّذ

لا شيء أدناه شُغِّل. مشروط بنجاح كل البوابات **و**P1.

| المرحلة | ما يُفعَّل | ما يُراقَب | rollback |
|---|---|---|---|
| 0 | `trust_boundary_enabled` وحده (مراقبة) | `trust.observed` في الأثر أسبوعًا → **يستبدل P1** | إطفاء العَلَم |
| 1 | `sparse_diagnostic_state` | حجم الحالة، استمرارية الجلسات | إطفاء — الجلسات المكتوبة تظل مقروءة (مُختبَر) |
| 2 | `trust_boundary_enforce` — فقط إذا كان رقم المرحلة 0 مقبولًا | إنذارات كاذبة، تذاكر محجوبة | إطفاء |
| 3 | الاسترجاع خلف عَلَم رابع، 1% → 5% → 25% → 50% → 100% | اتفاق الظل عند كل نسبة | إطفاء |

**قاعدة الانتقال:** لا انتقال لنسبة أعلى مع أي انحدار مهم. **وأي فشل يعيد العمل للمرحلة المسؤولة، لا يُصلَح أثناء التفعيل.**

---

## 8. ما لم يُنفَّذ أو لم يُثبَت — صراحةً

| البند | الحالة |
|---|---|
| تشغيل الظل على ترافيك حقيقي | **لم يُشغَّل**. الآلية جاهزة ومُختبَرة |
| نسبة الإنذار الكاذب على إنتاج | **غير مقيسة** — 0.00% على وكيل من 327 رسالة اختبارية |
| فهرس مدعوم بقاعدة بيانات لـ100,000 | **لم يُنفَّذ** — الشرط الوحيد المتبقي |
| أرقام التوقيع المفصّل (facets) | **غير مُعاد قياسها** — من المولّد القديم |
| LLM كمصنِّف | **غير مُثبَت** |
| تحلل الأدلة ومسار `contradicts` | **لم يُنفَّذ** — العيب 7، جذر T1 |
| تسلسل الأدوار / idempotency | **لم يُنفَّذ** — خصم كوتة مزدوج ما زال ممكنًا |
| صلاحيات `sie_customer_memory` | **لم يُلمس** — تغيير قاعدة بيانات إنتاج |
| حلقة `unclear` بلا عدّاد · بوّابة النشر المتجاوَزة | **لم تُصلَحا** |
| «أقبل للتطوّر» | **حجّة بنيوية لا قياس** |

---

**تنبيه أخير.** ثلاثة من قياساتي في هذا العمل كانت خاطئة وكانت مقنعة تمامًا، وكُشفت في كل مرة **بتناقض مع تنفيذ مُشاهَد** لا بمراجعة ولا باختبار أخضر. 856 اختبارًا تثبت أن ما قِيس يتصرف كما وُصف — لا أكثر. وقد تسرّبت سلاسل هجوم من هذا المستودع إلى مجموعة «الترافيك المشروع» **ثلاث مرات** بثلاث قواعد استبعاد مختلفة قبل أن أستبدل الاستنتاج بإعلان صريح.
