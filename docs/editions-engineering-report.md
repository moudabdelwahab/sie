# التقرير الهندسي — إصدارات SIE (Free · Pro · Max)

> **التاريخ:** 2026-09-24 · **الفرع:** `claude/compassionate-franklin-nh9b7p` (مدفوع، بدون PR)
> **الحالة:** كل شيء في بيئة الاختبار. **لم يُطبَّق أي تغيير على الإنتاج** — لا migration، ولا نشر، ولا تغيير إعداد. الاستعلامات على قاعدة الإنتاج كانت قراءة فقط.
> **الاختبارات:** 1000/1000 · فحوص SQL 50/50 · اختبارات الطفرات 10/10 · تدقيق الإصدارات 0 نتائج.
> **نموذج التهديدات:** [`docs/security/editions-threat-model.md`](security/editions-threat-model.md)

---

## 0. الخلاصة

| | المطلوب | المُسلَّم | السبب |
|---|---|---|---|
| Free | ≤ 650 | **635** | 15 تكرارًا حقيقيًا دُمجت بلا فقدان وصول |
| Pro | ≤ 1000 (+350) | **835** (+200) | توقّف عند نهاية سطح المنصة الموثَّق للعميل — الباقي كان سيكون تكرارًا أو ادعاءات غير مُتحقَّقة |
| Max | ≤ 1500 (+500) | **843** (+8) | النواة تغطي معظم «العام والشائع» أصلًا؛ اتكتب 10 وشِيل 2 لأنهم تكرار، بالدليل |

**الحكم:** المحرك الواحد بثلاث ملفات تعريف شغّال ومُختبَر، والإصدار الأكبر **مايقدرش** يبقى أسوأ من الأصغر — ده مفروض بقواعد تدقيق في CI، وبحارسين وقت التشغيل، وبمقارنة على 6,181 رسالة. لكن الأرقام المطلوبة (1000/1500) **ما اتحققتش**، وده قرار مقصود: شرطك إن مفيش تكرار وإن كل سيناريو يضيف قيمة مُثبتة أقوى من الرقم. وفيه نتيجة مهمة لازم تتقال بوضوح: **مفردات Pro ضيقة** — الصياغات اللي كتبتها بإيدي بتنجح 200/200، لكن صياغات مستقلة محجوزة (held-out) بتنجح **15/52 بس (29%)**، وMax **1/8**. Pro بيضيف تغطية حقيقية ومش بيكسر حاجة، بس أقل مما يوحي به الرقم 200/200.

---

## 1. ما اتبنى

**محرك واحد، ثلاث ملفات تعريف** — مش ثلاث محركات.

| الجزء | الملف | ماذا يفعل |
|---|---|---|
| ملفات التعريف | `sie/editions/editions.js` | حدود كل إصدار (السيناريوهات، طول الرسالة، مرشحو الاسترجاع، توكنات الأدلة، المعدل، السقف الشهري) مقيدة بحدود هندسية، وأحادية الاتجاه Free ≤ Pro ≤ Max |
| الحزم | `sie/scenarios/packs/src/{pro,max}/*.mjs` ← `scripts/build-packs.mjs` | صيغة تأليف (DSL) تتحول لنفس JSON النواة. تراكمية: Pro = نواة + pro، Max = نواة + pro + max |
| طبقات المعجم | `sie/language/normalizer.js` | تُطبق **فقط** على الكلمات اللي المعجم الأساسي ماحلّهاش ← ثقة كل سيناريو أساسي متطابقة في كل إصدار |
| تجميع الكتالوج | `sie/editions/edition-catalog.js` | تحميل كسول لكل حزمة (Free مابيجيبش Pro)، حارس الحزم، هوية مصفوفة ثابتة للكاش |
| الدور الواحد | `sie/editions/edition-turn.js` | حد التوكنات، نطاق الاسترجاع، **أرضية Free** و**حارس الكلمات العامة** — مشتركة بين الـ pipeline والجسر (نفس الكود في الإنتاج والاختبار) |
| التدقيق | `sie/editions/edition-audit.js` + `stand-off.js` | كل قاعدة تقرر هل الحزمة تُشحن (§4، §6) |
| الموازِن | `scripts/pack-rebalance.mjs` | يوزّن التوقيعات لتحقيق قواعد التدقيق؛ ما يستحيل تحقيقه يُبلَّغ ولا يُفرض |
| الجسر | `sie-integration/sie-chat-bridge.js` | الإصدار من قاعدة البيانات فقط؛ فشل تحميل حزمة ← Free مع تسجيل ذلك |
| قاعدة البيانات | `sie-integration/migrations/0009_sie_editions.sql` | مكتوبة ومختبرة، **غير مطبقة** (§11) |
| اللوحة | `sie-admin/settings.html` | قسم «الإصدارات» (§10) |

---

## 2. تدقيق الـ 650 (قبل أي إضافة)

- **جرد حقيقي:** `scripts/data/core-intents.tsv` — مفتاح نية لكل سيناريو من الـ 650.
- **التكرار:** 15 زوجًا نفس الحالة للعميل ← دُمجت مع نقل توقيع المحذوف كـ `alternativeSignature` للباقي، فالثقة على أي رسالة = max(القديمين): **لا صياغة فقدت وصولها**. الـ ids المحذوفة في `aliases.json` عشان الجلسات المخزنة والأثر يتحلّوا.
- **عيوب هيكلية:** تعادل بين قراءة وقراءة أكمل منها كان يُعتبر «غموض» ← الأكمل يفوز الآن (قاعدة الخصوصية)، مع استثناء `unknown`. إجابات أسئلة التمييز كانت لا تتحول لأدلة أصلًا ← أُصلحت (أثر إنتاج حقيقي: `debited_only` كانت تفتح تذكرة `subscription_expired`).
- **حقائق خاطئة:** **29 إجابة أساسية** كانت تقول سلوكًا يناقضه كود مدعوم نفسه (إلغاء/تخفيض ذاتي، مرفقات 25MB، الرد يعيد فتح التذكرة، ساعات دعم ثابتة، دفع بالكارت فقط، واجهة webhook للعميل، دعوة أعضاء بالإيميل، أكواد SMS، حد 200×200 للصورة، «30 دقيقة» للقفل، حذف مفتاح API، حذف الحساب، لغة الواجهة لكل حساب…) — كلها مصححة من مصدر موثق في `scripts/catalog-fixes/2026-09-core-audit.mjs` (قابل لإعادة التشغيل، idempotent).
- **أثر ذلك على Free (مقاس على 6,275 رسالة):** 6,077 بلا تغيير، 29 صارت صحيحة، 133 تعادلًا انحلّ؛ الإجابة الصحيحة 27.9% ← 30.3% (والآن 30.8%).
- **إصلاح استخلاص الذاكرة:** «عايز اغير اسمي اللي ظاهر» كانت تحفظ الاسم «اللي ظاهر». تغيّر Free في 5 أدوار من 6,275، كلها تحسن.

---

## 3. الإصدارات: ماذا أضاف كل منها

### Pro — 200 سيناريو (14 دفعة)
التذاكر كما يراها العميل، التذاكر مع الدعم، الحساب واللوحة، الفوترة والمحفظة، المساعد، الإشعارات والواتساب، الشركة وAPI والنطاقات الفرعية، التفعيل والمحادثة ومركز المساعدة، التقارير والأمان، البوابة والموظفين والحملات، المطورين وMCP والاشتراكات و2FA، أوضاع الشات بوت وتعريف الشركة، الدخول والجلسات، المدونة وتفضيلات العرض. **كل حقيقة من ملف في ريبو Mad3oom** مذكور في رأس كل دفعة.

**لماذا 200 وليس 365:** خريطة صفحات مدعوم الموجهة للعميل مقابل النواة + Pro (دفعة 14) لم تترك إلا المدونة والتفضيلات. ما استُبعد **بالدليل**: حالة CAPTCHA (`turnstile-config.js` موجود لكن `login.html` لا يحمّله — العميل لا يراه)، أداة إرسال البريد وصفحة WhatsApp Sender (أدوات إدارة/مطورين)، سجل العميل ولوحة المتابعة (طاقم).

### Max — 8 سيناريوهات
مشاكل على جهاز العميل وشبكته، بحقائق عن المتصفحات وأنظمة التشغيل وبدون أي ادعاء عن المنصة: ملء تلقائي لكلمة مرور قديمة، تكبير الصفحة، إصدار المتصفح، انقلاب النص العربي/الإنجليزي عند اللصق، رفض الأرقام العربية، شبكة الشغل/الفايروول، صفحة دخول واي فاي عام، نافذة منبثقة ممنوعة.

**لماذا 8 وليس 500:** النواة (635، منها 205 سيناريو محادثة) تغطي معظم «العام»: متصفح غير مدعوم، إضافات، VPN، كاش، تحديثات، نت ضعيف، جهاز مستعار، سبام، إذن الإشعارات، التوقيت، شكل الموبايل، أعطال التطبيق، قارئ الشاشة، المساحة. واتكتب 10 وشِيل 2 **لأنهم تكرار**: «أسجّل الشاشة» = `ticket_how_to_record_screen` في Pro (التدقيق مسكه)، و«غلطت في الإيميل» = `email_wrong_address` في النواة (اتمسك بالقراءة — التدقيق **مامسكهوش**، §13). الاستمرار كان يعني إعادة صياغة النواة.

السيناريوهات الأساسية اللي كل حالة Max اتقارنت بيها: `ui_browser_not_supported`، `ui_extension_blocking`، `ui_vpn_blocking_access`، `ui_cache_causing_stale`، `ui_broke_after_update`، `platform_blank_page`، `convo_weak_internet`، `convo_borrowed_device`، `email_going_to_spam`، `email_wrong_address`، `notif_browser_permission_blocked`، `ui_timezone_wrong`، `ui_mobile_layout_broken`، `app_crashes_on_open`، `a11y_screen_reader`، `scale_storage_limit` — وفي Pro: `ticket_how_to_record_screen`، `ticket_how_to_take_screenshot`، `security_2fa_code_rejected_clock`.

---

## 4. منع التكرار (غير قابل للتفاوض)

**اختبار CI دائم:** `sie/scenarios/tests/catalog-uniqueness.test.mjs` — التدقيق لازم يطلع **0 نتائج** على Free وPro وMax معًا، والحزم المترجمة متزامنة مع مصادرها. يغطي:

| الفحص | يمسك |
|---|---|
| `identical_vector` / `same_token_set` | نفس التوقيع أو نفس مجموعة التوكنات |
| `semantic_duplicate` | أربع إشارات: التسمية العربية والإنجليزية والإجابة والمعجم |
| `same_answer` | إجابتان متشابهتان ≥ 0.8 — حالتان وقرار واحد |
| `intent_overlap` / `duplicate_intent` / `duplicate_id` | نفس (الموضوع+الفعل) أو نفس المعرّف |
| `synonym_token` / `pattern_collision` / `dead_pattern` / `layer_redefines_base` | مفردات متنافسة أو ميتة أو مخطوفة |
| `not_decisive` / `core_displaced` | سيناريو لا يُوصل إليه، أو سيناريو أساسي فقد وصوله |
| `single_token_competition` / `word_set_competition` | «تنافس بلا فرق سلوكي» (§6) |

الطريق الوحيد لتمرير زوج مُعلَّم: `reviewed-pairs.json` بسبب مكتوب. **وهل الفحوص نفسها تعمل؟** `audit-rules.test.mjs` يغذّي كل قاعدة بحزمة تخالفها فوق النواة الحقيقية، ويشترط أن تنطلق (لأن اختبار «0 نتائج» لا يفرّق بين كتالوج نظيف وقاعدة محذوفة).

---

## 5. المقارنة Free × Pro × Max — نفس الرسائل

6,181 رسالة (مدوّنة السلوك: مسابير من النواة + ضوضاء + رسائل من الريبو)، كل رسالة في كل إصدار بنفس مسار الجسر:

| | Free | Pro | Max |
|---|---|---|---|
| إجابات صحيحة (الهدف) | 62.6% | 62.6% | 62.6% |
| نتائج غامضة | 1,861 | 1,857 | 1,856 |
| تذاكر | 1,424 | 1,420 | 1,420 |
| إجابات | 2,314 | 2,319 | 2,320 |
| فقدان إجابة صحيحة | — | **0** | **0** |

**كل فرق Free ← Pro مُفسَّر** (`node bench/edition-compare.mjs --list …`):
- 6,164 متطابقة.
- 5 غموض انحلّ (قراءة Pro أدق: «ولازم اسجل دخول كل مره» ← التحقق بخطوتين كل مرة).
- 13 «سيناريو مختلف» — **ولا واحدة غيّرت الفعل**: 10 سؤال توضيح في الإصدارين، و3 تذكرة في الإصدارين؛ ولا واحدة كانت إجابة صحيحة في Free (بوابة: 0 فقدان صحيح). أغلبها قراءة أدق («فauto renewal» ← التجديد قبل الانتهاء، «ملاحظة: لو اللينك مجاش» ← لينك تغيير كلمة المرور، «الدعم الفني» ← قنوات التواصل). **واتنين أسوأ:** «باسورد غلط كده» ← «بيانات حساسة اتبعتت في تذكرة» بدل «تغيير كلمة المرور»، و«العميل طلب موظف» ← «عضو الشركة مش قادر يدير الأعضاء». في الحالتين السؤال بلا سؤال تمييز محدد، فالعميل بيشوف **نفس نص التوضيح العام** اللي في Free؛ الفرق في الفرضية الداخلية المتصدرة (اللي ممكن تأثر على الدور التالي أو مسودة التذكرة) — مُسجَّل كحد متبقٍ (§13).
- **1 غموض جديد:** «تيليجرام: بيقبل السر الصح» — العميل يأخذ **نفس السؤال بالظبط** (`tg_token_source`)؛ تغيّر سبب القاعدة الداخلي فقط (R8 ← R6).
- «إجابات جديدة» خاطئة كانت 4 («القدرة المقفولة…» ← «الرد على تذكرة مقفولة»)، اتصلحت بحارس الكلمات العامة (§6) فصارت 0.

**Pro ← Max:** 6,182 متطابقة، 1 غموض انحلّ، 0 تراجع.

**على الصياغات المحجوزة (held-out):** Pro مش أكثر أثرًا من Free في أي منها؛ التذاكر 15 ← 9، والغموض 28 ← 16.

---

## 6. الغموض — لم يزد، وإزاي اتضمن ده

الغموض كان أكبر خطر حقيقي. أول قياس لـ Pro: **+94 غموضًا جديدًا، +161 تذكرة، 10 تراجعات**. السبب: كلمة عامة واحدة («مرفوض»، «مشكلة») بتدّي سيناريوهات Pro نفس ثقة القراءة الأساسية، فسؤال Free يبقى تذكرة في Pro. الإصلاح طبقات:

1. **قاعدة الكلمة الواحدة عند كل حضور ممكن** (`stand-off.js`). الحضور 0.8 للكلمة العامية و0.75 للعربيزي، وكل الفجوات بتتضرب فيه؛ فجوة 0.11 عند 1.0 تبقى 0.088 عند 0.8. اكتشفه اختبار تكافؤ الهجمات («انا صاحب الحساب ومش عارف ادخل»). المدى 0.75–1 **مُثبت** إنه المدى الحقيقي (لا دليل «مناقض» في الإنتاج، وnoisy-OR لا يخفض) ومثبّت باختبار.
2. **قاعدة مجموعات الكلمات:** كل مجموعة من كلمتين أساسيتين أو أكثر داخل توقيع حزمة تُفحص **بالترتيب الحقيقي للمحرك** (هامش، خصوصية، احتواء) على الإصدار مقابل الإصدار الأدنى. 9 نتائج اتصلحت بإعادة تأليف.
3. **المقارنة مع الإصدار الأدنى مباشرة:** Max ضد Pro، لا ضد Free فقط.
4. **أرضية Free (وقت التشغيل):** تذكرة/تصعيد سببه الغموض ومعاه سيناريو حزمة في الثلاثة الأوائل ← يُعاد القرار على السيناريوهات الأساسية وحدها ويُحتفظ بالأقل أثرًا. في الـ pipeline **وفي الجسر**. تنطلق على **0** من 6,181 رسالة عادية (أزالت الإصلاحات الأسباب من المصدر)، وتفضل حزام أمان: تعطيلها يُسقط هجوم «فرض الفعل».
5. **حارس الكلمات العامة (وقت التشغيل):** 22 توكن كلمات يومية في Pro وواحد في Max («اوقف»، «موقوف»، «كتبته»، «مقفولة»، «لينك»…) كانت تجاوب حالة بعينها لوحدها («اوقف» ← إيقاف التحقق بخطوتين). القائمة تُشحن داخل الحزمة؛ إجابة سيناريو حزمة كل أدلته كلمات عامة يُعاد قرارها. «اوقف التحقق بخطوتين» تفضل تجاوب.
6. **بوابة عدم التراجع** (`edition-no-regression.test.mjs`، CI): Pro/Max على كامل المدوّنة: 0 فقدان صحيح، 0 تغيير تفسير، غموض/تذاكر/إخفاقات ≤ Free.

---

## 7. الأداء

كل كتالوج في عملية منفصلة بعد تسخين، 3 تشغيلات (أدنى/وسيط/أعلى)، ms لكل دور:

| الكتالوج | الحجم | p50 | p95 | p99 | المسح الكامل p99 | مرشحون p95 | الحالة p95 |
|---|---|---|---|---|---|---|---|
| Free | 635 | 0.26/0.26/0.27 | 0.44/0.51/0.64 | 0.74/0.81/1.03 | 1.53/1.68/1.97 | 41 | 12.9 KB |
| Pro | 835 | 0.26/0.27/0.28 | 0.50/0.55/0.64 | 0.79/0.86/1.07 | 1.62/1.97/2.27 | 51 | 14.8 KB |
| 1,000 | 1,000 | 0.27/0.28/0.29 | 0.53/0.68/0.68 | 0.86/1.12/1.30 | 2.49/2.68/3.92 | 61 | 17.3 KB |
| 1,500 | 1,500 | 0.28/0.30/0.30 | 0.60/0.71/0.77 | 0.96/1.10/1.23 | 3.06/3.40/3.86 | 91 | 26.2 KB |

- الاسترجاع المحدد النطاق **مطابق** للمسح الكامل لـ Free (0 فرق في 10,694 دور)، وهو ما يجعل 1,500 سيناريو تكلّف تقريبًا ما تكلّفه 650.
- ذيل Pro (p95/p99) ضمن تذبذب القياس من Free. عند 1,500 يرتفع الوسيط p99 من 0.81 إلى 1.10 ms — أعلى من Free-scoped لكن **ثلث** تكلفة المسح الكامل عند نفس الحجم، وأقل مما يدفعه الإنتاج اليوم.
- البدء البارد 70–115 ms، الذاكرة ≈ 2 MB. **حالة الجلسة تكبر** مع الكتالوج (p95 12.9 ← 26.2 KB عند 1,500).
- 1,000 و1,500 **محشوة بسيناريوهات اصطناعية** من المفردات الحقيقية بتوزيعها الحقيقي — لقياس التكلفة فقط، لا تُشحن.
- الملفات: `bench/results/edition-perf-2026-09-24-r2.jsonl`.

---

## 8. الأمن (التفاصيل في نموذج التهديدات)

12 تهديدًا، لكل واحد: السبب الجذري ← الإصلاح ← اختبار الانحدار ← إعادة الهجوم. الأهم:

- **مجموعة هجمات جديدة:** 46 هجومًا (24 من مدوّنة الثقة + 22 موجهة لمحتوى Pro: مفاتيح API، MCP، 2FA، نصوص مزوّرة، حقن معرّف سيناريو وMarkdown، تسريب داخلي، تسريب بين المستأجرين، فيضان مفردات) × 12 تحويرًا (أحرف صفرية، اتجاه، تطويل، تشكيل، أحرف متشابهة، عرض كامل، أحرف تحكم، خلط لغات، تكرار، 50,000 حرف) × Pro وMax × الثقة enforce/off = **2,208 مقارنة**. وجدت 4 ثغرات حقيقية — كلها اتصلحت (§6).
- **طبقة الثقة في المسار لكل سيناريو جديد:** نفس مستوى الثقة في Pro/Max مثل Free على كل هجوم (مُختبر).
- **حارس الحزم:** حدود هيكلية + فحص محتوى (روابط/Markdown/HTML/نطاقات خارج مدعوم/طلب بيانات اعتماد/معرّفات داخلية) على كل إجابة في النواة والحزم **والصفوف المنشورة من اللوحة**.
- **تصعيد الإصدار:** سياسات RLS منسوخة من الإنتاج، و7 فحوص تعمل **كعميل**: لا يرفع إصداره ولا حصته ولا يضيف صفًا ولا يغيّر الإصدار الافتراضي. إصدار مزوّر في `botState` مُتجاهَل. إصدار مجهول ← Free.
- **خارج النطاق (مُبلّغ، لم يُعدّل):** صلاحيات Supabase الافتراضية تعطي `anon/authenticated` صلاحية `TRUNCATE` على جدولين (RLS لا تغطيها؛ غير قابلة للاستغلال عبر PostgREST). والصفوف السبعة المنشورة في الإنتاج كلها غير صالحة للمخطط.

---

## 9. اختبار الاختبارات

| ما فُحص | النتيجة |
|---|---|
| اختبارات بلا تأكيد (تنجح دائمًا) | 0 من 971 (`test-quality.test.mjs`، CI) |
| نصوص هجوم داخل مدوّنة «شرعية» | ملف المدوّنة نفسه كان مستثنى بالمسار فقط ← صار مُعلنًا صراحة؛ لا تسرّب |
| مدوّنة المقارنة ملوثة بهجمات | لا |
| **معيار غير ممثِّل** | **34 من 195 صياغة Pro مطابقة لتسمية السيناريو، و51 تشاركها ≥ 75% من كلماتها** ← صياغات محجوزة مستقلة: Pro **15/52**، Max **1/8** (أرضيات مثبّتة، لا يُضبط عليها) |
| **اختبارات طفرات** (`scripts/mutation-check.mjs`) | 10 كسرات متعمدة لسطر واحد. **أول تشغيل: 7 ماتت و3 نجت** — حد طول الرسالة وحد التوكنات في الـ pipeline لم يكن لهما اختبار، واختبار «الحزمة لا تستبدل سيناريو أساسيًا» كان يستخدم `find()` فينجح مع نسخة ثانية. اتصلحوا ← **10/10** |
| قواعد التدقيق تعمل؟ | كل قاعدة تنطلق على حزمة مخالفة (`audit-rules.test.mjs`) |

---

## 10. لوحة الإدارة (`sie-admin/settings.html`)

قسم «الإصدارات» بالعربي وRTL: ثلاث بطاقات (مجاني/احترافي/أقصى) بإعدادات أساسية ظاهرة وتفاصيل متقدمة مطوية، واختيار الإصدار الافتراضي، واختيار إصدار لكل عميل في درج الوصول (يظهر فقط بعد وجود العمود في القاعدة). يتحكم في: أقصى عدد سيناريوهات، الاستهلاك الشهري، المعدل في الدقيقة والدفعة، أقصى طول للرسالة، حد مرشحي الاسترجاع، حد توكنات الأدلة.

**حواجز الإعداد الخطر** (`edition-guards.js`، مُختبرة وبطفرة): رفض Pro أقل من Free أو Max أقل من Pro؛ تأكيد قبل خفض السيناريوهات، أو رسالة < 1,000 حرف، أو استرجاع < 30، أو معدل < 30/دقيقة، أو سقف شهري، أو تغيير الإصدار الافتراضي؛ قيم معدل 1–9 مرفوضة (0 = يرث). كل قيمة مقيدة بحد هندسي في `editions.js`. لقطات شاشة سطح المكتب والموبايل (فاتح/داكن) اتفحصت بلا تجاوز أفقي.

---

## 11. قاعدة البيانات — migration `0009` (غير مطبقة)

تضيف عمود `edition` على `customer_sie_access`، وسقفًا شهريًا لكل إصدار داخل `sie_consume_message` (ذرّي)، وطبقة الإصدار في حد المعدل (استثناء العميل > الإصدار > العام) عبر دالة واحدة مشتركة، و`p_edition` في `sie_admin_set_access`، وتسحب صلاحية `anon`. **مبنية على دوال الإنتاج الفعلية** (تختلف عن `0008` في الريبو، التي تجعل المستخدمين بلا حد). مختبرة على Postgres 16 محلي: 50 فحصًا منها مقارنة قبل/بعد مع دوال الإنتاج.

---

## 12. ما لم يُطبَّق على الإنتاج — وترتيب النشر المقترح

لا شيء طُبّق. وقبل أي نشر، القرار لك:

1. **تغييرات تغيّر Free نفسه** (مقاسة، كلها تحسين): دمج 15 تكرارًا، قاعدة الخصوصية، أسئلة التمييز كأدلة، 29 تصحيح حقيقة، إصلاح الذاكرة، فحص محتوى الصفوف المنشورة (لا أثر اليوم). مش مرتبطة بالإصدارات — ممكن تتنشر لوحدها.
2. **الإصدارات نفسها** خاملة لحد: (أ) تطبيق `0009`، (ب) رفع تثبيت نسخة المحرك في Edge Functions لمدعوم، (ج) تعيين إصدار لعميل. قبل ذلك كل العملاء Free بالسلوك الحالي بالظبط.
3. المقترح: نشر (1) ← مراقبة ← تطبيق (2) مع `default_edition = free` ← تفعيل Pro لعميل تجريبي واحد ← قراءة `trace.ranking.engine.floor` في الأثر.

---

## 13. الحدود المتبقية (بلا تجميل)

1. **ضيق المفردات:** Pro 15/52 وMax 1/8 على صياغات مستقلة. الخطوة الأكثر قيمة بعد ذلك ليست سيناريوهات أكثر، بل مرادفات أوسع — باستخدام مجموعة تطوير **منفصلة** عن المجموعة المحجوزة.
2. **فحص التشابه الدلالي** لا يمسك حالتين بنفس المعنى بتسميات مختلفة حين يكون أحدهما بلا نص إجابة (`email_wrong_address`). المراجعة البشرية تفضل مطلوبة.
3. **أغلب سيناريوهات الحزم بلا أسئلة تمييز**: الأرضية تعيد سلوك Free عند التعادل بدل أن تسأل سؤالًا خاصًا بـ Pro.
4. قاعدة مجموعات الكلمات تفترض حضورًا موحّدًا داخل المجموعة؛ الحضور المختلط مغطّى بالمدوّنة وبالأرضية، لا بإثبات.
5. **ادعاءات أساسية غير مُتحقَّقة** (لم تُعدّل — استبدال تخمين بتخمين ليس إصلاحًا): `subscription_invoice_missing`، `billing_seat_limit_reached`، دمج/تعديل جماعي للتذاكر، `ui_feature_request`، `billing_account_suspended_nonpayment`، `compliance_erasure_request` («حذف بيانات العميل» غير موجود في لوحة الشركة)، `data_restore_deleted` (استرجاع خلال 30 يومًا بلا مصدر).
6. حالة الجلسة تتضاعف تقريبًا عند 1,500؛ ومقاييس 1,000/1,500 على حشو اصطناعي.
7. حد معروف موثّق ومُختبر: «مجتمع مدعوم ده ايه» يُقرأ كحديث عابر.
8. معدلات كشف طبقة الثقة هي معدلات Free — الإصدارات لا تحسّنها ولا تسوئها (مُثبت).
9. فحص المحتوى نمطي: تصيّد بلا رابط وبلا طلب صريح غير مغطّى.
10. قراءتان أسوأ في Pro من غير تغيير في نص الرد (§5): «باسورد غلط كده» و«العميل طلب موظف» — الفرضية الداخلية المتصدرة تختلف عن Free.
11. الإصدار الأكبر ممكن يسأل بدل ما يجاوب لما سيناريو حزمة يعادل قراءة أساسية (حالة واحدة على المدوّنة، نفس السؤال).

---

## 14. كيف تعيد كل رقم

```bash
npm test                                             # 1000 اختبار
node scripts/audit-editions.mjs                      # 0 findings
node scripts/check-phrasings.mjs                     # 200/200
node bench/edition-compare.mjs --from free --to pro  # §5 (و pro→max، free→max)
node --expose-gc bench/edition-perf.mjs --only pro   # §7 (free | pro | pad1000 | pad1500)
node scripts/mutation-check.mjs                      # 10/10 KILLED
PGURL=… scripts/test-migrations.sh                   # 50 فحص SQL
```
