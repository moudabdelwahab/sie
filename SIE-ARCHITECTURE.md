# SIE — Architecture

> **الحالة:** وثيقة معمارية مرجعية · تصف النظام **كما هو اليوم**، وتُسمّي صراحةً ما نُفِّذ (§13–14) وما اقتُرح ولم يُنفَّذ.
> **النطاق:** `moudabdelwahab/sie` · فرع `claude/sie-architecture-audit-6q7sev` · مشروع Supabase `srnelrdpqkcntbgudyto`.
> **قاعدة القراءة:** كل رقم هنا **مقيس** أو **مشتق رياضيًا**. ما ليس كذلك مُعلَّم `[فرضية]` أو **غير مُعاد قياسه**.
> **§15 يسرد ما كان خاطئًا في النسخة الأولى من هذه الوثيقة وكيف اكتُشف.** اقرأه قبل الاستشهاد برقم قديم.
> **للمهندس الجديد:** اقرأ §1 ثم §4 ثم §6. الباقي مرجع.
>
> **ما تغيّر في الكود منذ النسخة الأولى** — كله خلف أعلام مقفولة افتراضيًا إلا ما وُسم إصلاحًا:
> `sie/trust` (الطبقة العاشرة) · `sie/retrieval` (فهرس معكوس) · `sie/diagnostics/sparse-state.js` ·
> سقف مدخل في `normalize()` **(إصلاح)** · ترتيب حلّ الهوية في `channel-identity` **(إصلاح)** ·
> تحييد الترميز في `MEMORY_REPLIES` **(إصلاح)** · `R6C` في محرك القرار (مقفول) ·
> `package.json` + CI + 805 اختبارًا.

---

## 1. Architecture Overview

SIE محرك تشخيص **حتمي** يحوّل رسالة عميل إلى قرار واحد من مجموعة مغلقة، ويُفسِّر قراره.
ليس منتجًا مستقلًا: هو طبقة الذكاء داخل منصة مدعوم، يشارك نفس قاعدة البيانات.

**الخاصية المعمارية المركزية اليوم — وهي أصل معظم ما في هذه الوثيقة:**

> المحرك **يسجّل كل سيناريو في الكتالوج، في كل دور**، ثم يرتّب النتائج.
> لا توجد مرحلة استرجاع (retrieval). التكلفة — زمنًا وذاكرةً وتخزينًا — **خطّية في حجم الكتالوج**.

هذا كان قرارًا سليمًا تمامًا عند 20 سيناريو. وهو القيد الذي يحكم كل شيء عند 650.

> **تحديث:** القيد لم يعد بنيويًا. `sie/retrieval` (§14) يجعل التسجيل مقصورًا على المرشحين **بتكافؤ مُبرهَن ومُختبَر**، و`sparse-state` (§5.3) يفصل حالة الجلسة عن حجم الكتالوج. الاثنان **مُنفَّذان ومقيسان**، والثاني خلف علم مقفول. النتيجة المقيسة: **10,000 صار قابلًا للوصول**، و**100,000 لا** — لحدّ ذاكرة لا لحدّ تمثيل (§14.3).

### 1.1 ما هو قوي بنيويًا (لا يُلمس)

| الخاصية | الدليل |
|---|---|
| اتجاه اعتماد أحادي | رسم استيراد كامل: لا ملف في `/sie` يستورد `sie-integration` أو `channels` أو `supabase` |
| كاتب وحيد | كل كتابة عبر `executeDecision` → RPC واحد → transaction واحد |
| حتمية كاملة | لا `Math.random()` في `/sie`؛ الوقت محقون عبر `clock`؛ فرز مستقر بكاسر تعادل صريح |
| بيانات لا كود | 650 سيناريو + 523 مدخل معجم في JSON خلف `create*Provider(loadFn)` |
| قابلية تفسير | كل قرار يحمل `evaluatedRules`: كل قاعدة فُحصت، ومطابقتها، وسببها |
| صفر تبعيات npm | 726 اختبارًا تعمل بلا قاعدة بيانات وبلا شبكة |

### 1.2 ما لا يتوسّع (مقيس)

| القيد | عند 650 | عند 10,000 | عند 100,000 |
|---|---|---|---|
| حالة الجلسة لكل دور | 206 KB | 3,073 KB | **30,991 KB** |
| زمن نواة القرار | 0.52 ms | 8.89 ms | **292 ms** |
| توقيعات متمايزة متاحة | كافٍ | **مستحيل** | **مستحيل** |

السطر الأخير ليس مسألة أداء بل **تمثيل**: انظر §5.

---

## 2. System Context

```text
 عميل على تيليجرام            عميل على الموقع
        │                            │
        ▼                            ▼
 Edge Fn: sie-channel-telegram   Edge Fn: sie-api
 service_role + webhook secret   JWT العميل يُمرَّر حرفيًا
 المحرك @ ab167ff                المحرك @ 820b79f   ◄── نسختان مختلفتان (B-01)
        │                            │
        └──────────┬─────────────────┘
                   ▼
          sie-runtime.js  (الواجهة العامة الوحيدة)
                   ▼
          sie-chat-bridge.js  (المنسّق · 1012 سطرًا · 13 مخرجًا)
                   ▼
          /sie  (تسع وحدات · لا تعرف شيئًا عن القنوات)
                   ▼
          Supabase (مشترك مع مدعوم)

 وعند كل cold start، بالتوازي:
          المحرك ──fetch()×4──► cdn.jsdelivr.net (1,011 KB)   ◄── تبعية وقت تشغيل
```

**حدود الثقة (trust boundaries):**

| الحد | ما يعبره | من يضمنه |
|---|---|---|
| تيليجرام → الدالة | سر webhook مُقارَن بزمن ثابت | `telegram-verify.js` — فشل مغلق عند غياب السر |
| المتصفح → sie-api | JWT العميل | `buildUserClient` + RLS في القاعدة |
| هوية القناة → حساب مدعوم | كود ربط أحادي الاستخدام | `channel_redeem_link_code` ذريًا + `UNIQUE(channel, channel_user_id)` |
| أي مسار → قاعدة البيانات | RLS + RPCs مقيّدة بالهوية | Postgres، لا JavaScript |
| **نص العميل → منطق المحرك** | **لا شيء** | **انظر §8 — هذا هو الحد الغائب** |

---

## 3. Component Architecture

| الطبقة | الملفات | الحالة الفعلية |
|---|---|---|
| Language / Normalization | `normalizer.js` (615) · `tokenizer` · `dialect-normalizer` · `response-language-policy` | موصول |
| Glossary | `technical-glossary.json` — 523 مدخلًا · 3,224 نمطًا · `arabizi-map.json` | موصول |
| طبقة محادثية | `small-talk.js` (~364 عبارة) · `emotion-detector.js` (~265) · `memory-intent.js` | موصول — **مفردات مكتوبة كـكود** |
| Typo tolerance | `typo-tolerance.js` | **كود ميت** — صفر مستوردين |
| Scenario Catalog | `scenarios.json` (650) · `scenario-types` · `.provider` · `.resolver` | موصول |
| Diagnostics | `diagnostic-engine` · `evidence-extractor` · `evidence-accumulator` · `hypothesis-tracker` | موصول |
| Live evidence | `live-evidence-provider.stub.js` | **stub دائم** — والكتالوج فيه **صفر** توكن `source:live` |
| Ranking | `ranking-engine.js` (153) | موصول |
| Decision | `decision-engine.js` (591) — R0…R9 | موصول |
| Dialogue | `dialogue-renderer` + `templates/{ar,en}` | موصول — **وأربعة نصوص عميل تعيش خارجه** في الـbridge |
| Knowledge | `answer-composer` · `static-knowledge.local` | موصول — **4 سيناريوهات فقط من 650** لها `knowledgeSource` |
| Live knowledge | `live-knowledge.stub.js` | **stub دائم** — `available:false` دائمًا |
| Action | `action-layer` · `supabase-port*` | موصول — الكاتب الوحيد |
| Observability 9a/9b | `trace-logger` · `trace-types` | موصول — **في مسار واحد من 13** |
| Observability 9c | `publish-gate` · `shadow-run` · `replay` · `comparison` · `validation-policy` · `observability-read-port` | **معمارية كاملة غير موصولة** |
| Config | `settings-schema.js` — 43 إعدادًا | موصول |
| CI / تبعيات | — | **غائب تمامًا** |

---

## 4. Execution Flow

التدفق الحقيقي **ليس** خطًا مستقيمًا. هو **ماكينة قرارات متسلسلة بثلاثة عشر مخرجًا**، سبعة منها تقفز فوق التشخيص بالكامل.

```text
رسالة
 ├─[قناة] verify → parse → dedupe(في الذاكرة) → identity → session   ← رحلات DB ١،٢
 │        └─ أي رسالة من 8 حروف/أرقام تُقرأ كـكود ربط (B-03)
 ▼ runSieTurn()
 0. getSieSettings()                                     كاش 60s
 1. tryConsumeSieMessage()                               ← رحلة DB ٣
    ══ الكوتة تُخصم هنا. كل فشل بعد هذا السطر مدفوع وغير مرصود ══
 2. recallPreviousState()  ·  3. pendingTicketConfirmation? → خروج ⬛
 4. recallPreviousSession()  (أول دور)                   ← رحلة DB ٤ · تسحب 206 KB
 5. normalize()
 6. smallTalk / emotion → تصعيد؟ → خروج ⬛
 7. memoryIntent → خروج ⬛      8. resolutionSignal → خروج ⬛
 9. كلام عادي → خروج ⬛
10. resolveScenarioCatalog()   11. processTurn()  ← 650 فرضية تُبنى من الصفر
12. rankDiagnosticState()      13. decide()  R0…R9
14. composeAnswerDecision()    15. rescueWithArticle()    16. renderDecision()
17. executeDecision()                                    ← رحلة DB ٥ · تكتب 206 KB
18. logTraceEvent()                                      ← رحلة DB ٦
    ══ المسار الوحيد من 13 الذي يكتب أثرًا، داخل try/catch صامت ══
19. رد ✅
 └─[قناة] saveState()  ← رحلة DB ٧ · **تكتب نفس الـ206 KB مرة ثانية**
```

**الرقم الحاكم:** 6–7 رحلات قاعدة بيانات متسلسلة لكل دور، أربع منها تحمل كتلة الـ206 KB، وواحدة زائدة تمامًا (`persist_bot_turn` يكتب `bot_state` بالفعل — متحقَّق من تعريف الـRPC الحي).

---

## 5. State Model

### 5.1 التصنيف

SIE **hybrid**: وحدات `/sie` **stateless نقية** (تأخذ الحالة السابقة وتعيد حالة جديدة بلا تعديل للمدخل)، داخل منسّق **stateful** يحتفظ بالحالة خارجها. هذا تصميم صحيح — المشكلة في **ما** يُخزَّن، لا **أين**.

### 5.2 أين تسكن الحالة

| الموضع | المحتوى | العمر | الحجم |
|---|---|---|---|
| `chat_sessions.bot_state.sie` | `diagnosticState` · `decisionState` · `pendingTicketConfirmation` · … | الجلسة | **206–222 KB** |
| `sie_customer_memory` | حقائق العميل | دائم | صغير — **ومعطّل على الويب** (§8) |
| `chat_engine_trace_events` | أثر الدور + `rawText` خامًا | دائم — **بلا سياسة احتفاظ** | ~9 KB |
| `settingsCache` (isolate) | 43 إعدادًا | 60s | **متغيّر عام مشترك** |
| `createMemoryDeduplicator` (isolate) | معرّفات معالجة | 10د أو حتى إعادة التدوير | 500 مدخلًا |
| `glossaryDerivationCache` | خمسة هياكل | عمر الـisolate | WeakMap — **أنظف كاش في النطاق** |

### 5.3 لماذا 206 كيلوبايت — ومُنفَّذ الآن

`updateHypotheses` يُنتج سجلًا **لكل سيناريو**، لا للمرشحين. مقيسًا على محادثة حقيقية بالكتالوج المشحون:

```
الدور 1   203.7 KB   650 فرضية، واحدة فقط بثقة > 0
الدور 3   207.1 KB   650 فرضية، 51 بثقة > 0

منها:  missingEvidenceTokens   ~37 KB
       history                 ~33 KB   ← لا يقرأها أحد في الإنتاج
       سجل الأدلة               0.2 KB   ← الشيء الوحيد غير القابل للاشتقاق
```

سجل الأدلة **0.1%** من الحمولة. الـ 99.9% الباقية كاش لدالة نقية، يُكتب في قاعدة البيانات كل دور.

**مُنفَّذ** في `sie/diagnostics/sparse-state.js`، خلف `sparse_diagnostic_state` (مقفول افتراضيًا):

| الكتالوج | الحالة الكاملة | الحالة المتفرقة | التقليص |
|---|---|---|---|
| 650 (الدور 1) | 203.7 KB | **0.34 KB** | ×591 |
| 650 (الدور 3) | 207.2 KB | 9.8 KB | ×21 |
| 10,000 | 2.82 MB | **0.65 KB** | ×4,436 |
| 100,000 | **28.44 MB** | **0.75 KB** | ×38,875 |

صف الـ 100,000 هو المهم: **28 ميجابايت حالة جلسة لكل محادثة** هو الحائط الذي يصطدم به الكتالوج أولًا — قبل المعالج، وقبل الاسترجاع.

**التوسيع دقيق (exact)** — كل حقل لكل سيناريو، مُتحقَّقًا دورًا بدور مقابل `updateHypotheses` نفسه لا مقابل تجهيزة. الوصول إلى ذلك تطلّب نقل خط واحد: المعيار البديهي «سبق أن كان نشطًا» يفقد أثر سيناريو نشط من الدور 3 لأن أثره يمتد إلى الدور 1. التتبّع من **أول ثقة غير صفرية** يجعل التوسيع دقيقًا بلا استثناء، لأن السيناريو غير المتتبَّع كان عند صفر في كل دور فسجله كله ثابت.

**ما لا يُصلحه:** المجموعة المتتبَّعة تنمو ولا تتقلّص (الـ hysteresis والأثر يحتاجانها). الحالة مقيّدة **باتساع المحادثة** لا بحجم الكتالوج — 79 متتبَّعًا و15.8 كيلوبايت بعد خمسة أدوار. قيد، لا ثابت، لكنه مستقل عن الكتالوج، وهو المطلوب.

### 5.4 عيوب الحالة

| # | العيب | الدليل | الحالة |
|---|---|---|---|
| 1 | **الأدلة بلا تحلل وبلا نقض** | `polarity` دائمًا `supports`؛ لا مسار ينتج `contradicts` من النص. الثقة تركم ولا تنقص داخل الجلسة | **مفتوح** |
| 2 | **لا تسلسل للأدوار** | لا قفل ولا رقم إصدار. رسالتان متقاربتان → lost update + خصم كوتة مرتين | **مفتوح** |
| 3 | **لا `schemaVersion`** | أي تغيير لشكل الحالة يكسر الجلسات الجارية بصمت | **مُصلَح** — `DIAGNOSTIC_SCHEMA_VERSION = 2`، والقراءة تقبل الشكلين |
| 4 | **مفتاح الكاش العام** | `settingsCache` بلا مفتاح مستأجر — غير مؤذٍ اليوم، تسريب فوري مع أول مستأجر ثانٍ | **مفتوح** |

العيب الأول هو سبب وجود `R6B_ALREADY_ANSWERED` وتصفير `closeConversation`: كلاهما **علاج على مستوى القرار لمرض في الطبقة الاستدلالية**.

---

---

## 6. Decision Model

```text
Evidence → Presence → Confidence → Ranking → Decision
```

**الثقة:**

```
confidence(s) = Σ presence(t)·w(t)  /  Σ w(t)      over t ∈ signature(s)
```

أربع خصائص تحكم كل شيء بعدها:

1. **نسبة تغطية لا قوة دليل.** توكن واحد حاضر من توقيع من توكن واحد = 1.0.
2. **لا تعاقب ما لا يُفسَّر.** سيناريو بـ3 توكنات مطابقة داخل رسالة بـ50 توكنًا = 1.0.
3. **لا تكافئ التخصيص.** `intent_how_to` (في 50 سيناريو) يساوي توكنًا فريدًا.
4. **المقام يعاقب التفصيل.** توقيع من 6 توكنات دقيقة يخسر لتوقيع من 2 عامّين — حافز معاكس، ومتوسط 2.87 دليل أنه عمل فعلًا.

**تأكيد من الإنتاج:** قيم `confidence` المسجّلة هي `0.5` · `0.64` · `0.6666666666666666` — كسور تغطية بسيطة، لا احتمالات معايرة. ومع ذلك `answer_confidence = 0.65` معروض للمشغّل كـ«مستوى التأكد». على توقيع من 3 توكنات بأوزان 4/1/1 القيم الممكنة هي {0, 0.17, 0.33, 0.67, 0.83, 1.0} — فـ**0.65 و0.60 و0.55 ثلاثة إعدادات تعطي نفس السلوك بالضبط**.

**الحتمية مؤكَّدة — لكنها ليست الصحة.** 16.8% من السيناريوهات تدخل تعادلًا تامًا يُحلّ بـ`a.scenarioId.localeCompare(b.scenarioId)`. النتيجة: **20 سيناريو لا يفوز أبدًا** حتى لو وُصف بدقّة.

**القواعد R0…R9** مرتّبة، أول مطابقة تفوز، وكل قاعدة تُسجَّل في `evaluatedRules`. هذا **أقوى موضع في النطاق كله** ويجب أن يبقى.

---

## 7. Scaling Model

### 7.1 كل ما يعتمد على عدد السيناريوهات

| الموضع | التعقيد | ملاحظة |
|---|---|---|
| `updateHypotheses` | O(S) زمنًا **وذاكرةً وتخزينًا** | يبني كائنًا لكل سيناريو |
| `rankHypotheses` | O(S log S) + O(S) تخصيص | فرز كامل + Map + map جديد |
| `bot_state` write/read | O(S) بايت × 4 رحلات | §5.3 |
| `validateCatalog` | O(S) عند cold start | مرة لكل isolate |
| تحميل الكتالوج | O(S) بايت من CDN | 811 KB @650 |
| `mergeScenarioCatalogs` | O(S) لكل دور **عند تشغيل الـoverlay** | مطفأ اليوم |
| `buildTraceEvent` | O(active) | فرضيات غير مهملة: 17–22 مقاسة |
| `normalize` | **مستقل عن S** | O(n²) في طول الرسالة — انظر §11 |

### 7.2 القياس — النواة لكل دور

| الكتالوج | الحالي (scan+sort) | vNext (retrieve+topK) | التسريع | مرشحون فُحصوا |
|---|---|---|---|---|
| 100 | 0.094 ms | 0.0038 ms | ×25 | 2 |
| 650 | 0.522 ms | 0.0171 ms | ×31 | 12 |
| 1,000 | 0.753 ms | 0.0076 ms | ×99 | 18 |
| 10,000 | 8.886 ms | 0.0303 ms | ×293 | 174 |
| 100,000 | **292.2 ms** | **0.3627 ms** | **×806** | 1,742 |

الحالي **فوق-خطي** (×100 في الكتالوج → ×389 في الزمن): ضغط GC من تخصيص 100,000 كائن لكل دور.
vNext **تحت-خطي** (×100 → ×47): التكلفة دالة في تواتر التوكنات المرصودة، والـstop-set يحدّ الأسوأ.

### 7.3 السقف الحقيقي: مفردات لا رياضيات

> **تصحيح.** النسخة الأولى من هذا القسم قالت إن التمثيل **يُشبع عند ~3,600 توقيعًا متمايزًا** وإن 10,000 و100,000 «غير قابلين للوصول». الرقم كان **أثرًا للمولّد**، لا خاصية للمحرك، والقسم أُعيد اشتقاقه بالكامل. التفصيل في §15.

المولّد الأول كان يسحب التوكنات **بإرجاع** من التوزيع الحقيقي. السحب بإرجاع لا يحدّ الرأس: التوكن الأكثر شيوعًا وصل إلى **47%** من الكتالوج المولَّد مقابل **7.7%** في الكتالوج الحقيقي، فنفدت المساحة لأن الرأس ابتلع المفردات.

الكتالوج الحقيقي **مؤلَّف لا مسحوب**: المحرّر يستعمل `intent_how_to` بعدد أسئلة «إزاي» ثم يتوقف. المولّد الآن **يبني منحنى التواتر** ثم يوزّعه (`bench/catalog-generator.mjs`)، فيطابق الشكل الحقيقي عند كل حجم.

**التمايز ليس هو السقف.** 100,000 توقيع متمايز **قابلة للبناء** من 516 توكنًا:

| N | تصادمات تامة (V = 516) | النسبة |
|---|---|---|
| 650 | 0 | 0.00% |
| 3,500 | 2 | 0.06% |
| 10,000 | 59 | 0.59% |
| 100,000 | 4,008 | **4.01%** |

**السقف هو التمييز (discriminability)** — أن تصل رسالة واقعية إلى سيناريو بعينه. `bench/quality-bench.mjs` يقيسه بأودّ مسبار ممكن: أقوى توكنين للسيناريو نفسه.

**أ) المفردات تنمو مع الكتالوج (V = 0.79N):**

| N | فوز% | تعادل% | ضائع% |
|---|---|---|---|
| 650 | 83.7 | 8.0 | 8.3 |
| 1,000 | 72.3 | 13.7 | 14.0 |
| 3,500 | 71.0 | 15.5 | 13.5 |
| 10,000 | 71.1 | 16.3 | 12.7 |
| 100,000 | 66.0 | 18.4 | 15.7 |

**ب) المفردات مثبّتة عند المعجم المشحون (V = 516):**

| N | فوز% | تعادل% | ضائع% |
|---|---|---|---|
| 650 | 77.1 | 11.2 | 11.7 |
| 3,500 | 55.1 | 23.6 | 21.3 |
| 10,000 | 46.3 | 28.3 | 25.4 |
| 100,000 | **24.6** | 28.1 | **47.4** |

> **تحقّق متقاطع:** الكتالوج المولَّد عند 650 يقيس **83.7%** فوزًا؛ الكتالوج **الحقيقي** عند 650 يقيس **84.3%** (548/650، من `catalog-reachability.test.mjs`). المولّد مُصدَّق مقابل الواقع، وليس مقابل نفسه.

**الاستنتاج المصحَّح:** السقف **معجمي لا تركيبي**. نمو الكتالوج بلا نمو المعجم لا يفشل — بل **ينهار تدريجيًا وبشكل مقيس**: عند 100,000 بمفردات اليوم، ثلاثة أرباع السيناريوهات لا يمكن الوصول إليها بكلماتها. النسبة الصحّية هي التي يحملها الكتالوج الحقيقي: **V/N ≈ 0.79**.

### 7.4 ما يفتح السقف

ثلاثة أشياء، بترتيب الأثر المقيس:

**1. المعجم ينمو مع الكتالوج.** أرخص تدخّل وأكبر أثر: 66% فوز بدل 24.6% عند 100,000. هذا عمل تأليف، لا كود.

**2. توقيع مفصّل (faceted)** — `(domain, topic, tokens)` — يجعل المساحة حاصل ضرب لا مجموعًا. القياس السابق (12 مجالًا × 8 مواضيع) أعطى عند 650: **100% فوز / 0% غموض** مقابل 94.8/5.8 مسطّحًا، وعند 1,000: 94.1% مقابل 82.5%.

> **حالة الدليل:** أرقام الـ facets أُخذت بالمولّد **القديم**، فهي صالحة اتجاهًا لا قيمةً، ولم تُعد بعد. مصنّفة **غير مُعاد قياسها**.

**3. مفسِّر ناقص يتفوق على مطابقة تامة مسطّحة.** في القياس نفسه: 91.4% بفاصل مستنتج بدقة 80% مقابل 82.5% بمطابقة توكنات مثالية. هذا هو المبرر المقاس لإدخال LLM **كمصنِّف لا كصانع قرار** — وهو أيضًا خاضع للتنبيه أعلاه.

**ما لم يعد صحيحًا:** «10,000 غير قابل للوصول». بمفردات متناسبة و«حالة متفرقة» (§5.5) و«استرجاع» (§14)، **10,000 قابل للوصول اليوم**: 71.1% فوز، 7 ميجابايت مقيمة، 0.47 ملّي ثانية أسوأ حالة، 0.65 كيلوبايت حالة جلسة. **100,000 ليس** — لا لأسباب تمثيلية بل لأن الفهرس والكتالوج يصيران ~95 ميجابايت مقيمة (§14.3).

---

## 8. Security Architecture & Threat Model

### 8.1 المبدأ: ما هو «حقن» في محرك حتمي

في نموذج لغوي، الحقن ينجح لأن **البيانات تدخل نفس القناة التي تدخل منها التعليمات**. المكافئ المجرّد في محرك حتمي:

> **أي مسار يجعل نص المستخدم يعبر من كونه مُدخَلًا إلى كونه جزءًا من حالة التحكم أو مخرَجًا للنظام.**

بهذا التعريف، SIE **لديه سطح حقن حقيقي**، ووجوده لا يحتاج LLM.

### 8.2 سجل التهديدات

| # | التهديد | كيف يحدث | ما يتأثر | الضرر | قائم؟ | المنع الجذري |
|---|---|---|---|---|---|---|
| **T1** | **تسميم الحالة عبر الأدوار** | الأدلة append-only بـnoisy-OR، بلا تحلل وبلا نقض؛ hysteresis تُبقي `active` | ranking · hypotheses · scenario selection | يشحن العميل جلسة بتوكنات فيتحكم في قرار دور لاحق | **نعم** | تحلل زمني + نافذة أدلة + مسار `contradicts` حقيقي |
| **T2** | **تلاعب الترتيب** | العميل يعرف/يخمّن عبارات المعجم فيرفع سيناريو بعينه | ranking · decision · actions | قيادة المحرك حتميًا إلى `ESCALATE` أو `CREATE_TICKET` | **نعم** | خصوصية (IDF) + عقوبة تغطية + حدّ لأثر الدور الواحد |
| **T3** | **تلويث الذاكرة وتزوير صوت النظام** | `MEMORY_REPLIES` يعيد نص العميل المخزَّن داخل قائمة نقطية للبوت؛ تيليجرام يرسل بـ`parse_mode: 'Markdown'` ولا شيء على المسار يهرّب | memory · chat_messages · أرشيف النزاعات | ملاحظة مخزَّنة فيها `[اضغط هنا](https://…)` تعود **رابطًا حيًّا** بصوت العلامة | **مُصلَح** | `neutralizeUserText` داخل `MEMORY_REPLIES` نفسه (لا عند المنادي) + قائمة مفاتيح مغلقة على مستوى المخزن |
| **T4** | **تسميم عبر الجلسات** | `recallPreviousSession` يسحب `diagnosticState` من جلسة أخرى لنفس المستخدم | hypotheses | أدلة أمس تقود قرار اليوم | **نعم** (`memory_use_past_conversations=true`) | نقل ملخّص مُصنَّف لا حالة خام + انتهاء صلاحية |
| **T5** | **تسميم الكتالوج** | صف منشور يطابق `id` سيناريو أساسي فيحلّ محله؛ **والـPublish Gate متجاوَز تمامًا** (`publishScenarioVersion` ينادي الـport مباشرة) | scenario selection · نص الرد لكل العملاء | تغيير ما يقوله البوت للجميع | مكتوم بـ`use_published_scenarios=false` فقط | توصيل `decidePublishability` + مراجعة ثانية + توقيع إصدار |
| **T6** | **صلاحية النشر = صلاحية نشر كود** | `is_chat_engine_staff()` → `role in ('admin','support')` | الكتالوج · `sie_settings` | أي مستخدم بدور `support` يعيد كتابة إجابات المحرك وإعداداته بلا بوّابة | **نعم** | فصل دور `catalog_publisher` + بوّابة + سجل تدقيق |
| **T7** | **تسميم حدّ المعدل** | `sie_rate_limit_hit(p_client_ip)` قابل للتنفيذ من `anon` و`p_client_ip` نص يوثَق حرفيًا | routing · availability | استنزاف دلو أي IP + **كتابة صفوف غير محدودة بلا مصادقة وبلا تنظيف** | **نعم** | `revoke from anon` + الـIP من الطلب لا من المنادي + cron تنظيف |
| **T8** | **استنزاف CPU بمدخل واحد** | لا سقف لطول المدخل في أي مكان بين webhook القناة والمحرك، و`normalize()` فوق-خطية | availability | مقيس: 9.8 KB = 29.8 ms · 49 KB = 224 ms · **196 KB = 2,697 ms** في `normalize()` وحدها، مقابل ميزانية edge بالمئات من الملّي ثانية | **مُصلَح** | `MAX_INPUT_CHARS = 8000` داخل `normalize()` — حدّ مورد لا سياسة، فلا يتوقف على طبقة الثقة |
| **T9** | **دور مزدوج وخصم مزدوج** | لا `Idempotency-Key`؛ dedupe في ذاكرة الـisolate؛ latency 2.4–3.5s | quota · actions | رد مكرر وتذكرة مكررة وخصم مرتين | **نعم** | مفتاح idempotency مخزَّن + dedupe مشترك |
| **T10** | **إغراق التذاكر** | كل «أيوه» يفتح تذكرة؛ `ticketAlreadyCreated` لكل جلسة، والجلسات غير محدودة | actions | إغراق طابور الدعم | **نعم** | حدّ تذاكر لكل عميل لكل نافذة زمنية |
| **T11** | **حرمان من الخدمة بالكوتة** | حلقة `unclear` في تأكيد التذكرة بلا عدّاد | quota | استهلاك رصيد العميل دورًا بدور | **نعم** | عدّاد بحدّ ثم افتراض «لا» |
| **T13** | **اختطاف رسالة الدعم بكود الربط** | `extractLinkCode` كان يطابق `/^[A-Z0-9]{8}$/` **قبل** حلّ الهوية | routing · identity | عميل مربوط يكتب «password» أو «whatsapp» فتُستهلك رسالته كمحاولة ربط فاشلة ويُرد عليه «الكود غلط» | **مُصلَح** | مطابقة أبجدية السكّ الحقيقية (بلا 0/O/1/I/L) + الكود المجرّد يُقرأ **بعد** أن يثبت أن المرسِل غير مربوط |
| **T12** | **تلويث طابور التعلّم** | `rawText` يُخزَّن خامًا وإلى الأبد، ويغذّي مراجعة بشرية وتحسين الكتالوج | learned data · feedback | إدخال نصوص مصمَّمة لتوجيه قرارات المراجعين | كامن — لا يوجد مسار تعلّم آلي بعد | فصل قناة الاقتراح عن قناة الاعتماد + بوّابة بشرية + سياسة احتفاظ |

### 8.3 نتائج سلبية — فُحصت ولم تُوجد

الدقة تقتضي ذكر ما بحثت عنه ولم أجده:

- **`formatTicketDescription` لا يُسرّب نص العميل الخام.** يبني الوصف من `scenarioId` و`category` و`explanation` والأثر فقط — كلها من إنتاج المحرك. فلا حقن محتوى في التذاكر عبر هذا المسار.
- **`review-center.js` يهرّب HTML** (`escapeHtml` مستخدَمة على الحقول المشتقّة من المستخدم). لا XSS مخزَّن عبر هذا المسار.
- **ReDoS في المعجم ممنوع بنيويًا:** كل أنماط المعجم تمر بـ`escapeRegExp()`، والأنماط بيانات لا مُدخَل مستخدم.
- **prototype pollution محمي:** `hasOwnProperty.call` في `resolveArabizi`.
- **`sie_consume_message` و`sie_admin_*` مفتوحة لـ`anon` لكنها تفشل مغلقة** داخليًا. سطح غير مرغوب، ليس ثغرة.
- **لا ثغرة عزل مستأجرين — لأن لا مستأجرين.** النظام أحادي المستأجر بالكامل. يجب توثيق ذلك قبل أن يبني أحد منطقًا خاصًا بعميل فوق تصميم لا يعرف المستأجرين.

### 8.4 عيوب مؤكَّدة

**أ) صلاحيات `sie_customer_memory` — مفتوح.**
الجدول عليه **`SELECT` و`DELETE` فقط** — لا `INSERT` ولا `UPDATE`.
فـ`rememberFacts` **يفشل صامتًا على مسار الموقع** (JWT العميل) وينجح على تيليجرام (`service_role`).
ميزة موعودة في لوحة الإعدادات تعمل على قناة ولا تعمل على أخرى، بلا خطأ ظاهر.
*لم يُلمس: إصلاحه تغيير في قاعدة بيانات الإنتاج، وهو خارج نطاق هذا العمل.*

**ب) 71.5% من الكتالوج قابل للحسم بتوكن واحد — مفتوح بنيويًا.**
مقيس: **465 من 650** سيناريو يبلغ عتبة الحسم 0.6 من توكن واحد، لأن الثقة **نسبة تغطية** على توقيعات أرقّ من أن تميّز. التوكن `intent_how_to` وحده يرفع **11** سيناريو فوق العتبة دفعة واحدة، و`isAmbiguous` يبلغ `false` لأنه يقيس فجوة الاثنين الأولين لا التوزيع.

القاعدة `R6C_NON_DISCRIMINATING_EVIDENCE` تجعل ذلك **ظاهرًا في كل أثر**، وتستطيع المنع — لكنها **مقفولة افتراضيًا**، لأن القياس يقول إن الفصل مستحيل: الحدّ الأقصى المشروع هو **11** أيضًا، تبلغه جملة «عايز اعرف عن منصه ازاي بتشتغل». المجتمعان **متطابقان لا متداخلان**. أي سقف يمنع المسبار يمنع سؤالًا معقولًا.

الإصلاح الحقيقي توقيعات بفواصل كافية للتمييز (§7.4)، لا عتبة.

**ج) اختبار الوصول للكتالوج كان فارغًا — مُصلَح.**
كان يتحقق أن مُعرّف السيناريو موجود في `ranking.ranked`. لكن `rankHypotheses` يعيد **الكتالوج كله** مرتّبًا — 650 مدخلًا منها 649 بثقة صفر — فالتأكيد كان صحيحًا بالبناء **ولا يمكن أن يفشل**. نجا لأن كل شيء عداه كان حقيقيًا: تطبيع عربي حقيقي، أدلة حقيقية، كتالوج حقيقي. السطر الأخير وحده كان فارغًا.
الآن يؤكد ثقة غير صفرية ورتبة ذاتية ضمن 20، ومُتحقَّق بالطفرة: تضييق الحدّ إلى 1 يُفشله بـ102 سيناريو مُسمّى.

### 8.5 استراتيجية مضادة للحقن — مُنفَّذة

لا تكفي validation نصية. المطلوب **حدود معمارية**. الأربعة أدناه صارت `sie/trust` (§13):

1. **حجر المدخل.** ✅ `admission-control` يصنّف الدور مرة واحدة ويُصدر `TrustEnvelope` يسافر معه.
2. **الحالة تخزّن إشارات لا نصًا.** ⚠️ جزئيًا — `note` و`lastCustomerText` ما زالا نصًا، لكن **عرضهما** صار محيَّدًا (T3) و**كتابتهما** صارت مقيَّدة بمفردات مغلقة ومحظورة تحت الثقة الكاملة.
3. **ميزانية أثر لكل دور.** ✅ `evidence-guard` — حساب لا كشف: دور مقيَّد لا يحرّك الاعتقاد أبعد من جملة عادية مهما صيغ.
4. **صوت النظام مفصول عن محتوى المستخدم.** ✅ `egress-guard` + `text-safety`.
5. **مفردات مغلقة للمفاتيح.** `key` في الذاكرة من مجموعة معرَّفة (`name`/`role`/`company`) — لا `note` حرّ.
6. **نشر الكتالوج فعل مُميَّز.** بوّابة + دور منفصل + سجل + قابلية رجوع.

---

## 9. Failure Model

`null` اليوم لا تفرّق بين ست حالات مختلفة كليًا:

```
null = المحرك مطفأ          → لا كوتة، لا مشكلة
null = غير مستحق             → لا كوتة، لا مشكلة
null = الكوتة نفدت           → لا كوتة، لا مشكلة
null = فشل كتابة             → كوتة مدفوعة، مشكلة حقيقية
null = الـCDN لم يرد         → كوتة مدفوعة، انقطاع تام
null = استثناء برمجي         → كوتة مدفوعة، خلل
```

ولأن `explainRefusal()` يقرأ صف الوصول **بعد** الخصم، عميل فشل دوره يُخبَر «خلصت رسايلك» — وصف صحيح للحالة وتفسير خاطئ للسبب.

| الحالة | السلوك اليوم | المطلوب |
|---|---|---|
| كتالوج فاسد | `validateCatalog` يتخطى المدخل ويُبقي الباقي ✅ | — |
| overlay غير متاح | ينزل للكتالوج المراجَع **ويسجّل السبب** ✅ | تنبيه |
| معرفة غير موجودة | يسقط لنص السيناريو ✅ | — |
| ذاكرة تالفة | `try/catch` → «لا ذاكرة» ✅ | — |
| قاعدة بطيئة/غائبة | `null` → المحرك التقليدي | تمييز + عدم خصم |
| كاش قديم | TTL 60s ✅ | — |
| مدخل خبيث | **لا سقف** ❌ | T8 |
| مدخل غامض | 12.2% غموض مقابل 12% تغطية بأسئلة تمييزية ❌ | §7.4 |
| تعادل في الدرجة | كاسر أبجدي ❌ | كاسر دلالي |
| النظام لا يعرف | `FALLBACK` — نص عام | تصعيد مفسَّر |
| فشل `send()` | 3 محاولات + jitter ✅ ثم يُفقد الرد بينما القاعدة تقول وصل ❌ | وسم عدم التسليم |
| حالة جلسة ضخمة | لا حدّ ❌ | §5.3 |

---

## 10. Observability

| القياس | القيمة |
|---|---|
| وحدات كوتة مستهلكة | **77** |
| أدوار مسجّلة في حالة الجلسات | **49** |
| آثار مكتوبة | **19** |
| جلسات SIE / منها لها أثر | **12 / 6** |

**~75% من الأدوار المدفوعة لا تترك أثرًا.** السبب بنيوي: `logTraceEvent()` في مسار واحد من 13، في أسفل الدالة، داخل `try/catch` يكتب `console.warn` ويكمل. **الفشل هو الحالة الوحيدة المضمون أنها لا تُسجَّل** — النقيض التام لما يحتاجه الرصد.

المحتوى نفسه ممتاز حين يُكتب: `rawText` · التوكنات · `hypothesesSnapshot` · `rankingSnapshot` · الـDecision كاملًا مع `evaluatedRules` · `processing_time_ms` · `action_result`. والفهارس الحية موجودة.

**الناقص:** أثر لكل دور · تصنيف فشل · تجزئة الـlatency · metrics · سياسة احتفاظ · تنبيه · CI.

---

## 11. Performance Model

| الطبقة | القياس |
|---|---|
| `normalize()` دافئًا، رسالة حقيقية | **0.06–0.11 ms** |
| البايبلاين كاملًا، دور دافئ | **0.9–11 ms** |
| الطبقة المحادثية | 0.12 ms |
| أول `normalize()` (بناء 5 هياكل) | 49.5 ms |
| cold start بلا شبكة | ~82 ms |
| **+ سحب من jsDelivr** | **1,011 KB عبر 4 نداءات** |
| **المرصود في الإنتاج** | **2,433–3,524 ms** |

> **حساب المحرك أقل من 0.5% من زمن الاستجابة المرصود.**

وأدق: الأثر الأخير (`2026-09-08 23:36:52 UTC`) = **3,524 ms**، والـcommit `ab167ff` (إصلاح المعجم) مُثبَّت عند `23:31:59 UTC`. **خمس دقائق بعد النشر، وأبطأ أثر مسجَّل.** والدور التالي على isolate دافئ يقينًا (بعد 5 ثوانٍ) = 2,433 ms — فالتفسير ليس cold start وحده.

`[فرضية تحتاج تحقق]` توزيع الـ>97% غير المفسَّر بين: نداءات الـCDN، و6–7 رحلات DB متسلسلة، وتسلسل 206 KB أربع مرات. **لا يمكن حسمها قبل تجزئة `processing_time_ms`** — وهي أول بند في Phase 1.

**تربيع في `normalize()`** (مستقل عن الكتالوج، دالة في طول الرسالة): `isTokenInsideAnyMatch` · `overlaps` · `foldNormalizedPhrases`. مقيس: 4 KB→6.5 ms · 28 KB→118 ms · 55 KB→447 ms. مقبول تحت سقف تيليجرام، مفتوح على الموقع.

---

## 12. Known Risks

| الخطر | متى يصير حادثًا |
|---|---|
| المحرك يتوقف عن الإجابة ويبدأ في فتح التذاكر | تدريجيًا مع كل 100 سيناريو — **وغير مرصود** |
| انقطاع تام من طرف ثالث | أي لحظة، أو يوم تحويل الريبو لخاص |
| فشل صامت غير قابل للتشخيص | **قائم** — 75% عمى |
| جوابان مختلفان لنفس السؤال حسب القناة | **قائم** — B-01 |
| دور وخصم مزدوجان | مع أول زيادة حمل حقيقية |
| تسريب بين مستأجرين | لحظة وجود مستأجر ثانٍ |
| انهيار التمييز مع نمو الكتالوج بلا نمو المعجم | **مقيس**: 77% فوز عند 650 → 46% عند 10,000 → 25% عند 100,000 بمفردات اليوم (§7.3) |
| حدود طبقة الثقة مُعايرة على وكيل لا على إنتاج | لحظة التفعيل بلا مرور بوضع المراقبة أولًا (§13.3) |

**تنبيه على قوة كل ما سبق:** كل الأدلة السلوكية تقوم على **19 أثرًا و3 عملاء مستحقين ومحادثة تيليجرام مرتبطة واحدة**. «نجح في الإنتاج» تعني هنا «لم يفشل في أقل من عشرين دورًا مرصودًا». المخاطر أعلاه لم تُرصد لأن **الحمل لم يبلغ الحد الذي يكشفها**، لا لأنها غير موجودة.

---

## 13. الطبقة العاشرة — حدّ الثقة (مُنفَّذة)

`sie/trust` — الحدّ الدفاعي بين المدخل المحادثي غير الموثوق وبين استدلال المحرك وحالته.

### 13.1 لماذا حدّ لا مرحلة

المعابر الأربعة **لا تقع في لحظة واحدة**: الأدلة تدخل مبكرًا، الحقائق تُكتب متأخرًا، الإجراءات تُصرَّح بعدها، والنص يُقتبَس في المخرَج. مرحلة عاشرة متزامنة كانت ستخمّن ثلاثة منها.

```
normalize ─► extract ─► [CP1 admit] ─► accumulate ─► rank ─► decide
                            │              ▲                   │
                       TrustEnvelope       │                   ▼
                            ├──────────────┘            [CP3b action]
                            ├──► [CP2 ميزانية الأدلة]            │
                            ├──► [CP3 كتابة الحقائق]             ▼
                            └──► [CP4 النص المقتبَس] ───────► الرد
```

**نقطة قرار واحدة، أربع نقاط تنفيذ.** المظروف يمكن أن **يضيق** لاحقًا ولا يتسع أبدًا — ثابتة `escalate` المُثبَّتة باختبار.

**موضع CP1 مُبرَّر لا مريح:** بعد التطبيع، لأن أكثر قياساته تمييزًا — عدد الإشارات التشخيصية المتمايزة — لا يوجد قبل أن يحلّ المعجم النص. بوّابة أمام طبقة اللغة كانت ستقرأ محارف خامًا، وهي بالضبط المصفاة الساذجة التي وُجدت هذه الطبقة كي لا تكونها. وقبل مسار الذاكرة، لأنه يكتب حالة دائمة ويعود دون أن يبلغ التشخيص.

### 13.2 لماذا هذا ليس مطابقة كلمات

مجسّان يحملان قوائم كلمات، والارتياب منهما مشروع. الفارق:

> القوائم تسمّي **ما يدور عنه النص**، لا **كيف يُصاغ الهجوم**.

`SPEAKER_ROLES` (مَن يدّعي النص أنه المتكلم)، `SYSTEM_SELF_REFERENTS` (إعدادات المساعد نفسه)، `PRIVILEGE_ROLES` (أدوار تغيّر المسموح) — مجموعات صغيرة وثابتة وقابلة للحصر لأن **المجال** يحدّها. قائمة عبارات غير محدودة لأن **المهاجم** يختار منها.

وهي **ليست حاملة للحِمل**: `signal_flood` و`domain_spray` وفحص الاتساع لا تقرأ كلمات أصلًا، وميزانية الأدلة **حساب لا كشف**. من يهزم كل المجسّات يبقى معه دور يتصرف كدور عادي.

### 13.3 المقيس

`npm run test:trust`. الحدود مُعايرة على 345 رسالة عربية من اختبارات المستودع نفسه (باستثناء ما يعيش في طبقة الثقة أو يستوردها).

- **18/18** هجومًا في مجموعة الهجمات مُلتقَط عند مستواه المطلوب أو أعلى
- **0/6** رسائل مشروعة تشبه الهجوم صُعِّدت
- **0/338** رسالة مشروعة صُعِّدت (0.00%)
- **29** اختبار ثوابت + **13** اختبار تكامل للأعلام

> **حدّ الدليل:** المجموعة نص اختبارات لا ترافيك إنتاج. نسبة الإنذار الكاذب مقيسة **مقابل هذه المجموعة** وهي حدّ أدنى. وضع `observeOnly` موجود ليستبدلها برقم حقيقي.

### 13.4 مجسّ حُذف بالدليل

`repetition_pump` صُمِّم ونُفِّذ وعُوير ثم **حُذف**. التكرار لا يحقق شيئًا أمام مركم noisy-OR: يُشبع عند التكرار الثالث (0.80 → 0.96 → 0.992)، وتوكن معجمي يُشبع من الأولى. المجسّ كان سيُطلق على تكرار **توقّف عن الدفع أصلًا**، بينما حدّه يقع فوق صياغتين عاديتين تمامًا: عميل يلصق سجلًا بأسطر مكررة، وعميل غاضب يكتب «مش شغال مش شغال».

الدفاع عن التكرار هو **حساب المركم نفسه** — غير مشروط، غير قابل للتهرب، ولا يحتاج كاشفًا.

---

## 14. الاسترجاع (مُنفَّذ)

`sie/retrieval` — فهرس معكوس يفصل تكلفة الإجابة عن حجم الكتالوج.

### 14.1 دقيق لا تقريبي

الثقة نسبة تغطية، فالتوكن الذي لم تنتجه الرسالة حضوره صفر ولا يساهم في البسط. إذن السيناريو الذي لا يشارك أي توكن **بسطه صفر وثقته صفر** — بالضرورة، لكل رسالة، بلا معايرة.

لذلك تسجيل السيناريوهات المشارِكة فقط يعطي **ثقات متطابقة وترتيبًا متطابقًا** مع مسح الكتالوج كله. ليس مقايضة دقة بسرعة، بل الحساب نفسه مع تخطّي الأصفار المُبرهَنة. `equivalence.test.mjs` يتحقق منه على كل توكنات التوقيع الـ516، و400 رسالة متعددة التوكنات، وأربعة كتالوجات مولَّدة بأشكال لا يملكها المشحون، وبالتأكد أن كل سيناريو **مُستبعَد** يسجّل صفرًا فعلًا.

### 14.2 المقيس

| N | مسح كامل | استرجاع (نمطي) | استرجاع (أسوأ) | مداخل مقروءة (أسوأ) |
|---|---|---|---|---|
| 650 | 0.13 ms | 0.055 ms | 0.027 ms | 49 |
| 3,500 | 0.61 ms | 0.005 ms | 0.111 ms | 270 |
| 10,000 | 1.69 ms | 0.019 ms | 0.466 ms | 770 |
| 100,000 | 27.6 ms | 0.062 ms | **4.955 ms** | 7,681 |

التكافؤ صمد في **الخلايا الخمس عشرة** كلها.

### 14.3 ما لا يُصلحه — والقيد الحقيقي

**التكلفة ليست ثابتة في N.** قائمة أكثر التوكنات شيوعًا هي 7.7% من الكتالوج عند كل حجم: 49 مدخلًا عند 650 و**7,681** عند 100,000. ثابت أفضل، لا تعقيد أفضل.

**القيد المُلزِم هو الذاكرة لا المعالج:**

| N | الكتالوج | الفهرس | المقيم |
|---|---|---|---|
| 10,000 | 2.5 MB | 4.1 MB | ~7 MB |
| 100,000 | 51.7 MB | 43.4 MB | **~95 MB** |

~95 ميجابايت غير قابلة للحياة في Supabase Edge Function بجانب حزمة 1,011 كيلوبايت والـruntime. وبناء الفهرس يكلّف **542 ms** على كل isolate بارد.

> **الحكم بالحجم:** **650 → 10,000 مريح** — 7 ميجابايت، 14 ms بناء، استرجاع دون الملّي ثانية. **100,000 غير قابل للوصول داخل العملية**: المعالج بخير، الذاكرة والبدء البارد لا. الوصول إليه يتطلب أن يتوقف الفهرس عن كونه مقيمًا — فهرس معكوس في قاعدة البيانات (GIN أو جدول postings) والاسترجاع استعلام. تغيير معماري لا تقوم به هذه الوحدة ولا ينبغي أن تدّعيه.

---

## 15. تصحيحات — ما كان خاطئًا في النسخة الأولى

هذا القسم موجود لأن الأرقام الخاطئة كانت **مقنعة**، وطريقة اكتشافها أفيد من الأرقام الصحيحة.

### 15.1 مجموعة المعايرة لم تكن عربية

استُخرجت بـ`grep -E "[\u0600-\u06FF]"`. GNU grep لا يدعم `\uXXXX`، فطابق المحارف الحرفية `\` و`u` و`0`-`6` و`F`. «المجموعة» من 1,077 «رسالة» كانت في معظمها عناوين اختبارات إنجليزية وشظايا كود.

كانت مقنعة: حجم معقول، مئينات معقولة، ونسبة إنذار كاذب 0.00%. كل حدّ اشتُق منها عُوير على ضجيج، وعدة حدود كانت نحو نصف ما يجب.

**ما كشفها تناقض لا مراجعة:** اختبار end-to-end فشل مبلّغًا **11** سيناريو فوق عتبة الحسم، على رسالة قالت المجموعة إن أقصاها **5**.

> **قياس يخالف تنفيذًا مُشاهَدًا هو قياس خاطئ، مهما بدت مئيناته.**

الاستخراج صار في JavaScript، والاختبار يؤكد وجود **رسالة كَناري** معروفة، فتفشل المجموعة الفارغة بدل أن تنجح.

### 15.2 «سقف المفردات عند ~10,107» / «الإشباع عند ~3,600»

أثر مولّد. السحب بإرجاع لا يحدّ الرأس: 47% فانأوت مولَّد مقابل 7.7% حقيقي. ببناء منحنى التواتر مباشرة، **100,000 توقيع متمايز قابلة للبناء** من 516 توكنًا بتصادم 4.01%.

السقف الحقيقي **معجمي**، ويظهر في التمييز لا التمايز (§7.3).

### 15.3 مجموعة «الترافيك المشروع» كانت تعدّ هجمات المستودع نفسه

الاستبعاد كان بالمسار — تخطّ `sie/trust/tests` — ونجح فقط ما دامت كل سلاسل الهجوم تسكن هناك. أول اختبار تكامل للأعلام في `sie-integration/tests` رفع النسبة من 0.00% إلى 0.30%.

واستبدال قاعدة المسار بقاعدة الاستيراد كان أسوأ: تجهيزة الهجمات في هذا المجلد تعلن 24 هجومًا **ولا تستورد شيئًا**، فارتفعت النسبة إلى 2.74%.

القاعدة الآن **الاثنتان معًا**. الدرس نفسه: قياس يتحرك بإضافة ملف اختبار غير ذي صلة لم يكن يقيس المحرك أصلًا.

### 15.4 `state_leverage` بصيغته الأولى غير قابل للتنفيذ

صُمِّم ليطلق حين يحمل دور واحد سيناريو فوق عتبة الحسم. لكن **91.5%** من الحسم المشروع يحدث على توكن داعم واحد — ذلك هو وضع التشغيل العادي للمحرك، لا الهجوم. المجسّ أُعيد إلى **الاتساع المطلق**، ووُثِّق صراحة أنه **لا يميّز عند الطرف الأدنى** (§8.4-ب).

---

## 16. قرارات معمارية (ADR)

| # | القرار | البديل المرفوض | لماذا |
|---|---|---|---|
| **1** | حدّ الثقة **حدّ** بأربع نقاط تنفيذ، لا مرحلة عاشرة | مرحلة متزامنة أمام الخط | المعابر الأربعة لا تقع في لحظة واحدة؛ مرحلة واحدة كانت ستخمّن ثلاثة منها |
| **2** | مستويات ثقة **متدرّجة** (trusted/constrained/quarantined/rejected) | سماح/منع ثنائي | حجب عميل مرتبك فشل أيضًا. النطاق المقيَّد يجعل الخطأ **رخيصًا**، وهو ما يمنع إطفاء الطبقة أول مرة تزعج أحدًا |
| **3** | التنفيذ **قائم على الأثر** (ميزانية أدلة) لا على الكشف | ثقة أكبر بالمجسّات | من يهزم كل مجسّ يبقى معه دور يتصرف كدور عادي. دفاع لا يتوقف على الكشف |
| **4** | CP2 يدخل `processTurn` كـ**دالة مُرشِّح** لا استيراد | استيراد طبقة الثقة في التشخيص | التشخيص يجب ألا يكتسب تبعية للثقة؛ رسم التبعيات بلا مخالفات طبقية وهو من أقوى ما في الكود |
| **5** | آلية `neutralizeUserText` في **طبقة اللغة**، والسياسة في الثقة | كلاهما في الثقة | `memory-intent` طبقة 1 ولا يستطيع الاستيراد من طبقة 10 دون قلب رسم التبعيات |
| **6** | سقف المدخل في `normalize()` **لا** في طبقة الثقة | الاعتماد على مجسّ الحجم | ذاك **سياسة** قابلة للإطفاء؛ هذا **حدّ مورد**، وحدّ مورد قابل للإطفاء ليس حدًّا |
| **7** | الاسترجاع **دقيق** لا top-K تقريبي | قطع بحد أعلى مُقدَّر | مُشتق لا مُعاير: بسط الصفر صفر. `limit` يقطع **بعد** التسجيل الدقيق |
| **8** | التتبّع المتفرق من **أول ثقة غير صفرية** لا من أول تنشيط | المعيار البديهي | المعيار البديهي يفقد أثر سيناريو نشط متأخرًا؛ هذا يجعل التوسيع **دقيقًا بلا استثناء** |
| **9** | الحالة تُضغط عند **الكتابة** وتُوسَّع عند **القراءة** | ترحيل دفعة واحدة | التفعيل بلا ترحيل، والتراجع يترك كل جلسة مكتوبة قابلة للقراءة |
| **10** | `R6C` و`maxSimultaneousResolvable` **مقفولان افتراضيًا** | تفعيلهما لإصلاح عيب حقيقي | القياس يقول إن المجتمعين متطابقان: أي سقف يمنع المسبار يمنع سؤالًا معقولًا. العيب يُسجَّل في الأثر ويُصلَح بالتوقيعات |
| **11** | ثلاثة أعلام، والتنفيذ **مرحلة ثانية** بعد المراقبة | علم واحد | الحدود مُعايرة على وكيل لا على إنتاج. وضع المراقبة هو كيف يُستبدل ذلك برقم حقيقي قبل أن يُرفَض شيء |

---

## 17. الوثائق المرافقة

- **`SIE-VNEXT-ROADMAP.md`** — إعادة تقسيم المراحل، والمعمارية المقترحة، وخطة التنفيذ حتى 100,000.
- **`docs/AUDIT-SIE-MAD3OOM-2026-09.md`** — تدقيق سابق للأمن والموثوقية والانحراف. أربعة من بنوده تحققتُ من إقفالها.
