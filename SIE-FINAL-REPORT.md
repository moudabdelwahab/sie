# SIE — تقرير نهائي

> **الفرع:** `claude/sie-architecture-audit-6q7sev` · 10 commits فوق `0d42bfb`
> **الاختبارات:** 805 ناجحة، صفر فاشلة · **بلا أي تبعية npm** · بلا تغيير في قاعدة البيانات أو الإنتاج أو `main`
> **قاعدة القراءة:** كل رقم هنا مقيس أو مشتق. ما ليس كذلك مُعلَّم **غير مُثبَت**.

---

## 1. ما بُني

| الوحدة | ماذا | الحالة |
|---|---|---|
| `sie/trust` | الطبقة العاشرة — حدّ ثقة بنقطة قرار واحدة وأربع نقاط تنفيذ | مُنفَّذة، خلف علم، **مقفولة** |
| `sie/retrieval` | فهرس معكوس، تكافؤ **مُبرهَن ومُختبَر** مع المسح الكامل | مُنفَّذة، غير موصولة بالترتيب بعد |
| `sie/diagnostics/sparse-state.js` | حالة جلسة متفرقة، توسيع **دقيق** | مُنفَّذة، خلف علم، **مقفولة** |
| `bench/` | مولّد كتالوجات واقعي + قياس أداء وجودة عند 650 → 100,000 | مُنفَّذة |
| `package.json` + CI | أول أمر تحقق واحد في المستودع | مُنفَّذة |

**إصلاحات مطبَّقة بلا علم** (عيوب مؤكَّدة، لا ميزات): سقف مدخل في `normalize()` · ترتيب حلّ الهوية · تحييد الترميز في `MEMORY_REPLIES` · قائمة مفاتيح مغلقة عند مخزن الحقائق · إصلاح اختبار وصول فارغ.

---

## 2. الطبقة العاشرة — ما تفعله فعلًا

المعابر الأربعة التي يعبر فيها نص المستخدم من **مُدخَل** إلى **حالة تحكم**:

```
normalize ─► extract ─► [CP1 admit] ─► accumulate ─► rank ─► decide
                            │              ▲                   │
                       TrustEnvelope       │                   ▼
                            ├──────────────┘            [CP3b action]
                            ├──► [CP2 ميزانية الأدلة]            │
                            ├──► [CP3 كتابة الحقائق]             ▼
                            └──► [CP4 النص المقتبَس] ───────► الرد
```

**ليست مطابقة كلمات.** قائمتا الكلمات تسمّيان *ما يدور عنه النص* (مَن يدّعي أنه المتكلم؛ إعدادات المساعد نفسه)، لا *كيف يُصاغ الهجوم* — مجموعات يحدّها المجال لا المهاجم. وهي **ليست حاملة للحِمل**: `signal_flood` و`domain_spray` والاتساع لا تقرأ كلمات، وميزانية الأدلة **حساب لا كشف**. من يهزم كل مجسّ يبقى معه دور يتصرف كدور عادي.

**المقيس:** 18/18 هجومًا مُلتقَط · **0/338** رسالة مشروعة صُعِّدت · 29 اختبار ثوابت · 13 اختبار تكامل.

**الحدّ:** المجموعة نص اختبارات لا ترافيك إنتاج. نسبة الإنذار الكاذب **حدّ أدنى**، ووضع المراقبة موجود ليستبدلها برقم حقيقي.

---

## 3. ثغرات حقيقية — وُجدت وأُصلحت

| # | الثغرة | الدليل | الإصلاح |
|---|---|---|---|
| 1 | **استنزاف CPU بلا سقف مدخل** | لا حدّ بين webhook والمحرك. مقيس: 196 KB = **2,697 ms** في `normalize()` وحدها، مقابل ميزانية edge بالمئات | `MAX_INPUT_CHARS = 8000` — حدّ مورد لا سياسة، فلا يتوقف على علم |
| 2 | **حقن روابط Markdown بصوت العلامة** | `MEMORY_REPLIES` يعيد نص العميل خامًا، وتيليجرام يرسل بـ`parse_mode: 'Markdown'`، ولا شيء يهرّب | `neutralizeUserText` داخل القوالب نفسها |
| 3 | **اختطاف رسالة الدعم بكود الربط** | `/^[A-Z0-9]{8}$/` قبل حلّ الهوية: «password» و«whatsapp» تُستهلك كمحاولة ربط | أبجدية السكّ الحقيقية + الكود المجرّد بعد إثبات أن المرسِل غير مربوط |
| 4 | **اختبار وصول الكتالوج كان فارغًا** | يؤكد وجود المُعرّف في `ranking.ranked` — وهي **الكتالوج كله**، 649/650 بثقة صفر. لم يكن يمكن أن يفشل | ثقة غير صفرية + رتبة ذاتية ≤ 20، مُتحقَّق بالطفرة |
| 5 | **مفاتيح الحقائق مفتوحة عند المخزن** | `rememberFacts` يكتب أي مفتاح يُسلَّم له | قائمة مغلقة عند الكتابة |

**فُحصت ولم تُوجد** (يُذكر كي لا يُبالَغ): `review-center.js` يهرّب HTML صحيحًا — **لا XSS**. `formatTicketDescription` لا يُسرّب نص العميل. ReDoS ممنوع بنيويًا. `role` لا يُشتق من النص في أي مكان.

**مفتوح ولم يُلمس:** صلاحيات `sie_customer_memory` (`INSERT`/`UPDATE` مفقودة) — إصلاحه تغيير في قاعدة بيانات الإنتاج، وهو خارج النطاق المصرَّح به.

---

## 4. النتائج السلبية — ما لم ينجح

هذا القسم أهم من قسم الإنجازات.

**أ) `repetition_pump` حُذف بعد بنائه ومعايرته.** التكرار لا يحقق شيئًا أمام noisy-OR: يُشبع عند التكرار الثالث (0.80 → 0.96 → 0.992)، وتوكن معجمي يُشبع من الأولى. المجسّ كان سيطلق على تكرار توقّف عن الدفع، بينما حدّه فوق صياغتين عاديتين. **الدفاع هو حساب المركم نفسه.**

**ب) `state_leverage` بصيغته المصمَّمة غير قابل للتنفيذ.** **91.5%** من الحسم المشروع يقع على توكن داعم واحد — وضع التشغيل العادي، لا الهجوم. أُعيد إلى الاتساع المطلق، **ووُثِّق أنه لا يميّز عند الطرف الأدنى**.

**ج) `R6C` يشحن مقفولًا رغم أنه يكشف عيبًا حقيقيًا.** **465 من 650** سيناريو (71.5%) قابل للحسم بتوكن واحد. لكن الحدّ الأقصى المشروع لعدد السيناريوهات فوق العتبة هو **11**، وأفضل ما يبلغه مهاجم بتوكن واحد هو **11** أيضًا — تبلغه جملة «عايز اعرف عن منصه ازاي بتشتغل». **المجتمعان متطابقان لا متداخلان.** أي سقف يمنع المسبار يمنع سؤالًا معقولًا. العيب يُسجَّل في الأثر؛ إصلاحه بالتوقيعات لا بالعتبات.

**د) ثلاثة قياسات كانت خاطئة، واحد منها في الوثيقة الأولى.** التفصيل في §5.

---

## 5. تصحيحات — وكيف اكتُشفت

**أ) مجموعة المعايرة لم تكن عربية.** `grep -E "[؀-ۿ]"` — GNU grep لا يدعم `\uXXXX`، فطابق المحارف `\` و`u` و`0`-`6` و`F`. 1,077 «رسالة» كانت عناوين اختبارات إنجليزية. **كانت مقنعة**: حجم معقول، مئينات معقولة، 0.00% إنذار كاذب.

> كشفها **تناقض لا مراجعة**: اختبار e2e فشل مبلّغًا 11 سيناريو فوق العتبة، على رسالة قالت المجموعة إن أقصاها 5. **قياس يخالف تنفيذًا مُشاهَدًا هو قياس خاطئ مهما بدت مئيناته.**

الحدود صُحِّحت (flood 14→18، ميزانية 4→8، اتساع 8→15)، والاستخراج صار في JavaScript، والاختبار يؤكد وجود رسالة كَناري.

**ب) «سقف المفردات ~10,107» و«الإشباع ~3,600» كانا أثر مولّد.** السحب بإرجاع لا يحدّ الرأس: 47% فانأوت مولَّد مقابل 7.7% حقيقي. ببناء منحنى التواتر، **100,000 توقيع متمايز قابلة للبناء** من 516 توكنًا بتصادم 4.01%.

**ج) مجموعة «الترافيك المشروع» كانت تعدّ هجمات المستودع نفسه** — مرتين، بقاعدتَي استبعاد كل منهما كشفت خطأ الأخرى.

---

## 6. المقيس — التوسّع

**الأداء** (`bench/retrieval-bench.mjs`):

| N | مسح كامل | استرجاع نمطي | استرجاع أسوأ | تسريع |
|---|---|---|---|---|
| 650 | 0.13 ms | 0.055 ms | 0.027 ms | ×2 |
| 10,000 | 1.69 ms | 0.019 ms | 0.466 ms | ×91 |
| 100,000 | 27.6 ms | 0.062 ms | 4.955 ms | ×446 |

التكافؤ مع المسح الكامل صمد في **الخلايا الخمس عشرة**.

**حالة الجلسة:**

| N | كاملة | متفرقة | التقليص |
|---|---|---|---|
| 650 (الدور 1) | 203.7 KB | 0.34 KB | ×591 |
| 10,000 | 2.82 MB | 0.65 KB | ×4,436 |
| 100,000 | **28.44 MB** | 0.75 KB | ×38,875 |

**الجودة** (`bench/quality-bench.mjs`) — نسبة فوز السيناريو بكلماته:

| N | مفردات متناسبة | **مفردات اليوم (516)** |
|---|---|---|
| 650 | 83.7% | 77.1% |
| 3,500 | 71.0% | 55.1% |
| 10,000 | 71.1% | **46.3%** |
| 100,000 | 66.0% | **24.6%** |

> **تحقّق متقاطع:** المولَّد عند 650 يقيس 83.7%؛ الكتالوج **الحقيقي** يقيس **84.3%**.

**الذاكرة:** عند 100,000 الكتالوج 51.7 MB والفهرس 43.4 MB = **~95 MB مقيمة**، وبناء الفهرس **542 ms** لكل isolate بارد.

---

## 7. هل المعمارية جاهزة لـ10,000 و100,000؟

### 10,000 — **نعم، بشرطين**، والأدلة مقيسة

| البعد | القياس | الحكم |
|---|---|---|
| المعالج | 0.47 ms أسوأ حالة | ✅ ليس القيد |
| الذاكرة | ~7 MB مقيمة | ✅ |
| حالة الجلسة | 0.65 KB | ✅ |
| البدء البارد | 14 ms بناء فهرس | ✅ |
| **الجودة** | **71.1%** فوز بمفردات متناسبة · **46.3%** بمفردات اليوم | ⚠️ **الشرط** |

**الشرطان:**
1. **المعجم ينمو مع الكتالوج** (V/N ≈ 0.79). بدونه يسقط الفوز إلى 46.3% — أي أكثر من نصف الكتالوج لا يُبلَغ بكلماته. هذا **عمل تأليف لا كود**، وهو أكبر عائق مقيس.
2. **وصل الاسترجاع بالترتيب.** الوحدة مُنفَّذة ومُبرهَنة لكنها **غير موصولة** — `rankDiagnosticState` ما زال يمسح الكتالوج. عند 10,000 هذا 1.69 ms، مقبول؛ لكنه يجب أن يُوصَل قبل النمو.

### 100,000 — **لا، ليس داخل العملية**

| البعد | القياس | الحكم |
|---|---|---|
| المعالج | 4.955 ms أسوأ حالة مقابل 27.6 ms مسحًا | ✅ **ليس القيد** |
| حالة الجلسة | 0.75 KB | ✅ |
| **الذاكرة** | **~95 MB مقيمة** | ❌ **القيد المُلزِم** |
| **البدء البارد** | **542 ms** بناء فهرس لكل isolate | ❌ |
| الجودة | 66.0% بمفردات متناسبة · 24.6% بمفردات اليوم | ❌ |

**السبب ليس تمثيليًا.** 100,000 توقيع متمايز قابلة للبناء، والمعالج بخير. القيد أن الكتالوج والفهرس **مقيمان** في isolate بسقف ذاكرة. الوصول إلى 100,000 يتطلب أن يتوقف الفهرس عن كونه مقيمًا — فهرس معكوس في Postgres (GIN أو جدول postings) والاسترجاع استعلام. **تغيير معماري لم يُنفَّذ هنا.**

---

## 8. ما لم يُنفَّذ أو لم يُثبَت — صراحةً

| البند | الحالة |
|---|---|
| وصل الاسترجاع بـ`rankDiagnosticState` | **لم يُنفَّذ**. الوحدة جاهزة ومُبرهَنة؛ الوصل لم يتم |
| فهرس مدعوم بقاعدة البيانات لـ100,000 | **لم يُنفَّذ** — هو الشرط الوحيد المتبقي لـ100,000 |
| أرقام التوقيع المفصّل (facets) | **غير مُعاد قياسها** — أُخذت بالمولّد القديم. صالحة اتجاهًا لا قيمةً |
| LLM كمصنِّف فواصل | **غير مُثبَت** — المبرر (91.4% بمفسِّر 80% مقابل 82.5% بمطابقة تامة) من القياس القديم نفسه |
| نسبة الإنذار الكاذب على ترافيك حقيقي | **غير مقيسة**. 0.00% مقابل وكيل من 338 رسالة اختبارية — حدّ أدنى |
| تحلل الأدلة ومسار `contradicts` | **لم يُنفَّذ** — العيب الجذري وراء T1 و`R6B` |
| تسلسل الأدوار / idempotency | **لم يُنفَّذ** — خصم كوتة مزدوج ما زال ممكنًا |
| صلاحيات `sie_customer_memory` | **لم يُلمس** — تغيير قاعدة بيانات إنتاج |
| حلقة `unclear` بلا عدّاد (T11) | **لم تُصلَح** |
| بوّابة النشر المتجاوَزة (T5) | **لم تُصلَح** |
| مقارنة الظل على ترافيك حقيقي | **لم تُشغَّل** — الآلية موجودة (`observeOnly` + `trust` في الأثر) ولم تُفعَّل |

---

## 9. الخطوة التالية الموصى بها

بالترتيب، وكل خطوة تُقاس قبل التالية:

1. **فعّل `trust_boundary_enabled` وحده** (مراقبة). اقرأ حقل `trust.observed` في الأثر أسبوعًا. **هذا يستبدل 0.00% الوكيلة برقم حقيقي** — ولا شيء بعده يُبنى قبله.
2. **فعّل `sparse_diagnostic_state`.** أقل الثلاثة خطرًا: التوسيع دقيق ومُختبَر، والتراجع يترك الجلسات المكتوبة قابلة للقراءة.
3. **فعّل `trust_boundary_enforce`** — فقط إذا كان رقم الخطوة 1 مقبولًا.
4. **صِل الاسترجاع بالترتيب** خلف علم رابع، بمقارنة ظل.
5. **نمِّ المعجم قبل الكتالوج.** أكبر عائق مقيس أمام 10,000، وهو ليس كودًا.
6. **أصلح تحلل الأدلة** — العيب الجذري الذي تعالجه قواعد القرار عرضيًا.

---

**ما لا يثبته هذا العمل:** أن CI خضراء تعني معمارية صحيحة. 805 اختبارًا تُثبت أن ما قِيس يتصرف كما وُصف. ثلاثة من قياساتي كانت خاطئة رغم أنها بدت مقنعة تمامًا، وكشفها في كل مرة **تناقض مع تنفيذ مُشاهَد**، لا مراجعة ولا اختبار أخضر.
