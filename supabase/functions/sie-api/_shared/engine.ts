/**
 * engine.ts — المحرك، مثبّت على كوميت واحد
 * ------------------------------------------------------------
 * الملف ده هو **المكان الوحيد** اللي فيه عنوان المحرك في الدالة دي.
 *
 * ── ليه مكان واحد ───────────────────────────────────────────
 * الـpin كان مكتوب في ملفين (index.ts و handlers/chat-reply.ts). طول
 * ما الاتنين على نفس الكوميت، Deno بيوحّد الرسم البياني وبيحمّل المحرك
 * مرة واحدة. أول ما واحد فيهم يتحدّث والتاني يتنسى، الدالة بتحمّل
 * **نسختين** من المحرك (٥٨٠ كيلو داتا لكل واحدة)، والأسوأ إن نفس
 * الطلب ممكن يشوف كتالوجين مختلفين. مرجع واحد بيمنع ده هيكليًا.
 *
 * ── ليه CDN مثبّت على كوميت مش فرع ──────────────────────────
 * فرع معناه إن أي push على الريبو بيغيّر المحرك تحت دالة شغالة، من
 * غير مراجعة ومن غير نشر. الكوميت معناه إن ترقية المحرك = تغيير السطر
 * ده + نشر، وده قرار مكتوب في التاريخ.
 *
 * ── إزاي تتأكد إن النشرة بتستخدم النسخة الصح ────────────────
 * الدالة بتعلن نسخة الرَنتايم اللي حمّلتها فعلاً في رد الصحة:
 *
 *   curl https://sie.mad3oom.com/api/v1/health
 *   → { "engine": { "runtime_version": "2.4.0", … } }
 *
 * الرقم ده جاي من الوحدة المحمّلة نفسها، مش من ثابت مكتوب هنا، فهو
 * دليل على اللي اتحمّل مش على اللي كنا ناويينه.
 *
 * ── التحديث ────────────────────────────────────────────────
 * ١. اعمل push للكوميت اللي فيه المحرك المطلوب.
 * ٢. غيّر SHA في السطر تحت (المكان الوحيد).
 * ٣. supabase functions deploy sie-api --project-ref <ref>
 * ٤. اتأكد من /api/v1/health إن runtime_version اتغيرت.
 *
 * الكوميت الحالي: e8ceb6f — أول كوميت فيه رَنتايم 2.4.0
 * (diagnoseMessage + طبقة مفاتيح الـAPI)، واللي الـAPI العام محتاجه.
 * قبله كان 6c8d164 (رَنتايم 2.3.0)، ومافيهوش الدوال دي.
 */
import * as sieRuntime from 'https://cdn.jsdelivr.net/gh/moudabdelwahab/sie@e8ceb6f42cf8cf20c65dcbfaf4eb0370769f5310/sie-integration/sie-runtime.js';

/** الوحدة كاملة — الـAPI العام بيمررها لـcreateApiPorts. */
export const runtime = sieRuntime;

/**
 * الدوال اللي المسارات القديمة بتستوردها بالاسم. مصدّرة من هنا عشان
 * تفضل على نفس الـpin من غير ما تكرر العنوان.
 */
export const { getSieReply, listActiveScenarios, SIE_RUNTIME_VERSION } = sieRuntime;
