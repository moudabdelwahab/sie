# SIE vNext — إعادة التقسيم المعماري وخطة الوصول إلى 100,000 سيناريو

> **الحالة:** مقترح معماري · **لم يُنفَّذ منه شيء** · ينتظر الاعتماد.
> **المرجع:** `SIE-ARCHITECTURE.md` — كل رقم هنا مأخوذ منه.

---

## 0. السؤال الذي يسبق كل شيء

سألتَ: هل المراحل التسع هي أفضل تقسيم هندسي ممكن؟

**الجواب: التقسيم سليم في حدوده، وناقص في محاوره.** كل وحدة من التسع لها مسؤولية واحدة واضحة وحدود نظيفة — وهذا نادر ويجب الحفاظ عليه. المشكلة ليست في وحدة موجودة، بل في **ثلاث مراحل غائبة** ومرحلتين مدموجتين خطأً.

### 0.1 مراجعة كل مرحلة

| المرحلة | المسؤولية | الحكم |
|---|---|---|
| **1 Language** | نص → توكنات معيارية | **تُقسَّم.** تفعل شيئين: تطبيع (صحيح) وتصنيف دلالي (غائب). مخرَجها حقيبة توكنات مسطّحة — فلا مرحلة بعدها تعرف «ما نوع هذه المشكلة» |
| **2 Scenarios** | كتالوج + تحقق + دمج overlay | **تبقى.** `resolver` أقوى إضافة حديثة |
| **3 Diagnostics** | أدلة → فرضيات | **تُقسَّم.** تفعل ثلاثة: تجميع الأدلة (يبقى) · **اختيار المرشحين** (غائب — تسجّل الكتالوج كله) · التسجيل (ينتقل) |
| **4 Ranking** | ترتيب + رصد غموض | **تُدمج مع 3.** التسجيل والترتيب فعل واحد على مجموعة مرشحين. فصلهما اليوم ينتج **عتبتين لا تتفقان** (`hypothesis-tracker` يستخدم 0.15 مدفونة، والترتيب يستخدم الإعداد) |
| **5 Decision** | قاعدة واحدة تفوز | **تبقى كما هي.** R0…R9 مع `evaluatedRules` أقوى موضع في النطاق |
| **6 Dialogue** | قرار → نص | **تبقى** — وتستعيد النصوص الأربعة التي تعيش في الـbridge |
| **7 Knowledge** | إثراء الإجابة | **تبقى** — نصفها stub |
| **8 Action** | الكاتب الوحيد | **تبقى كما هي** |
| **9 Observability** | أثر + طابور تعلّم | **تُقسَّم.** رصد وقت التشغيل ≠ مختبر تحقق ≠ تعلّم. والثلاثة مخلوطة، وثلثاها غير موصول |

### 0.2 المراحل الغائبة الثلاث

| المرحلة الغائبة | لماذا غيابها هو أصل المشكلة |
|---|---|
| **Interpretation** | لا أحد يسأل «ما نوع هذه المشكلة؟» قبل التسجيل. فيُسجَّل الكتالوج كله ويُترك للترتيب أن يكتشف المجال من الأرقام. وهذا مستحيل بنيويًا فوق ~1,000 سيناريو (§7.3) |
| **Retrieval** | **أخطر غياب.** لا مرحلة تضيّق المساحة. كل شيء خطّي في حجم الكتالوج: الزمن، الذاكرة، التخزين |
| **Learning** | لا مسار من مراجعة بشرية إلى تحسّن. `chat_engine_conversation_reviews` يجمع التصحيحات ولا شيء يعيدها |

### 0.3 التقسيم المقترح

```text
     اليوم (9)                         vNext (10)
─────────────────────       ─────────────────────────────────────
                            0  Ingress        حجر المدخل · حد الثقة   [جديد]
1  Language            →    1  Interpretation تطبيع + تصنيف مفصّل    [يتوسّع]
2  Scenarios           →    2  Catalog        + فهرس معكوس           [يتوسّع]
                            3  Retrieval      مرشحون قليلون          [جديد]
3  Diagnostics    ┐         4  Assessment     أدلة + تسجيل + ترتيب
4  Ranking        ┘    →                      على المرشحين فقط        [دمج]
                            5  Ambiguity      كسب المعلومة · أي سؤال؟ [جديد]
5  Decision            →    6  Decision       R0…R9 كما هي            [كما هي]
7  Knowledge      ┐    →    7  Composition    معرفة + حوار            [دمج]
6  Dialogue       ┘
8  Action              →    8  Commit         الكاتب الوحيد           [كما هي]
9  Observability  ┐    →    9  Trace          أثر لكل دور، من finally [يتوسّع]
                  └    →   10  Learning       خارج مسار الطلب تمامًا  [جديد]
```

المكسب ليس عدد المراحل، بل أن **٣ و٤ تعملان على عشرة مرشحين بدل 650**، وأن **٥ مسؤولية مستقلة** بدل قيمة منطقية داخل الترتيب.

---

## 1. المعمارية المقترحة

```text
          نص المستخدم  (غير موثوق — لا يعبر هذا الحد أبدًا)
                │
   ╔════════════▼════════════╗
   ║ 0 INGRESS               ║  سقف طول · dedupe مشترك · idempotency key
   ║   UntrustedText         ║  ═══ حد الثقة ═══
   ╚════════════┬════════════╝
   ┌────────────▼────────────┐
   │ 1 INTERPRETATION        │  تطبيع → Signal[] من مفردات مغلقة
   │   → Interpretation{     │  + domain/topic مستنتَجان  + polarity + negation
   │       signals, facets } │  [نقطة إدخال LLM الاختيارية — مصنِّف لا حاكم]
   └────────────┬────────────┘
   ┌────────────▼────────────┐
   │ 3 RETRIEVAL             │  فهرس معكوس: facets + signals → ~10 مرشحين
   │   ScenarioIndex(WeakMap)│  التكلفة دالة في الرسالة، لا في الكتالوج
   └────────────┬────────────┘
   ┌────────────▼────────────┐
   │ 4 ASSESSMENT            │  ScoringStrategy: تغطية × خصوصية × تفسير الرسالة
   │   على المرشحين فقط      │  EvidenceModel: تحلل + نقض + سقف أثر الدور
   └────────────┬────────────┘
   ┌────────────▼────────────┐
   │ 5 AMBIGUITY             │  أي سؤال يفصل المرشحين أكثر؟ (كسب المعلومة)
   └────────────┬────────────┘
   ┌────────────▼────────────┐
   │ 6 DECISION   R0…R9      │  بلا تغيير — على مجموعة صغيرة
   └────────────┬────────────┘
   ┌────────────▼────────────┐
   │ 7 COMPOSITION           │  معرفة + قوالب · صوت النظام معزول عن نص المستخدم
   └────────────┬────────────┘
   ┌────────────▼────────────┐
   │ 8 COMMIT                │  RPC واحد · transaction واحد · الحالة ~549 B
   └────────────┬────────────┘
   ┌────────────▼────────────┐
   │ 9 TRACE   من finally    │  أثر لكل دور مهما كان المخرج
   └────────────┬────────────┘
                ▼
   ╔═════════════════════════╗
   ║ 10 LEARNING (غير متزامن)║  اقتراحات → مراجعة بشرية → shadow-run → نشر مُبوَّب
   ║   لا يكتب في مسار الطلب ║
   ╚═════════════════════════╝
```

### 1.1 ما يبقى · يُفصل · يُعاد تصميمه · يُحذف

| | |
|---|---|
| **يبقى** | R0…R9 و`evaluatedRules` · Action كاتبًا وحيدًا · نمط `create*Provider` · `resolver` (base+overlay) · `catalog-integrity.test.mjs` · أمن القناة · حتمية كاملة |
| **يُفصل** | الـbridge → `TurnRunner` + `ConversationPolicy` + `SettingsToPolicy` · نصوص العميل → `dialogue/templates` · التسجيل → `ScoringStrategy` · نموذج الأدلة → `EvidenceModel` · مفردات small-talk/emotion → JSON |
| **يُعاد تصميمه** | تخزين الحالة (فرضيات → أدلة) · مقياس الثقة (تغطية → تغطية×خصوصية×تفسير) · موقع الرصد (نهاية النجاح → `finally`) · `null` → `{handled, reason}` |
| **يُحذف** | `createHttpSieClient` وتعليقات الـ501 · `scenario-catalog.supabase.js` · ملفات `migrations/` المضلِّلة (تُستبدل بـdump مرجعي) |
| **يُوصَّل** | `shadow-run` · `comparison` · `replay` · `publish-gate` · `typo-tolerance` · محوّل الموقع |
| **يُؤجَّل** | WhatsApp · Messenger · API كقناة · المرفقات · embeddings · تعدد المستأجرين |

---

## 2. القرار المعماري الحاسم

> **السؤال:** هل 10,000 و100,000 مسألة أداء أم مسألة تمثيل؟
> **الجواب المقاس: تمثيل.**

| | 650 | 1,000 | 10,000 | 100,000 |
|---|---|---|---|---|
| الأداء (vNext) | 0.017 ms | 0.008 ms | 0.030 ms | **0.36 ms** ✅ |
| الحالة (vNext) | 549 B | 549 B | 549 B | **549 B** ✅ |
| **التمييز (توقيع مسطّح)** | 16.8% تعادل | 10.5% تعادل | **مستحيل** | **مستحيل** ❌ |

الفهرس المعكوس وتقليص الحالة **يحلّان الأداء والتخزين بالكامل حتى 100,000** — مقيسًا.
ولا يحلّان التمييز إطلاقًا، لأن المفردات الفعّالة **148** توكنًا وحدّ عيد الميلاد يضع التصادمات عند N≈565.

**لذلك: التوقيع المفصّل ليس تحسينًا بل شرط وجود.** وقد قِيس أثره: عند 650 يرفع الفوز إلى 100% ويُنزل الغموض إلى 0%؛ وعند 1,000 إلى 94.1% و3.9%.

وفاصلان يشتريان رتبة عشرية واحدة تقريبًا. الطريق إلى 100,000 يحتاج **هرمًا يتعمّق مع الكتالوج**، لا فاصلين ثابتين.

---

## 3. خطة التنفيذ

كل مرحلة: هدف · ما تحله · الملفات · المخاطر · التحقق · الرجوع.

### Phase 0 — الرؤية والعدالة  ·  لا تلمس الذكاء

**الهدف:** أن نرى النظام ونوحّده ونتوقف عن محاسبة العميل على أخطائنا.
**تحل:** B-01 · B-02 · B-03 · T7 · T8 · T11 · W-05 · D-02

| # | العمل | الملفات |
|---|---|---|
| 0.1 | ثابت SHA واحد + اختبار يفرض تساوي كل الدوال | `supabase/functions/**` |
| 0.2 | `logTurn()` من `finally` + `{handled, reason}` بدل `null` | `sie-chat-bridge.js` · `sie-runtime.js` · `channel-adapter.js` |
| 0.3 | خصم الكوتة بعد النجاح (أو `sie_refund_message()`) | `sie-chat-bridge.js` · migration |
| 0.4 | `extractLinkCode` بعد حل الهوية | `channel-identity.js` |
| 0.5 | سقف 4,096 حرفًا داخل `normalize()` | `normalizer.js` |
| 0.6 | `revoke execute … from anon` + الـIP من الطلب + cron تنظيف | migration |
| 0.7 | عدّاد لحلقة `unclear` | `sie-chat-bridge.js` |
| 0.8 | `package.json` + GitHub Actions | جذر |
| 0.9 | إصلاح اختبار الوصول ثم رصد الـ20 المسدودة | `catalog-reachability.test.mjs` |
| 0.10 | وسم `migrations/` SUPERSEDED + dump مرجعي | `**/migrations/` |

**المخاطر:** 0.3 يلمس الفوترة — يُنفَّذ خلف علم مع مسار قديم قابل للعودة. 0.5 قد يقطع رسالة طويلة مشروعة — الحد 4,096 هو سقف تيليجرام القائم فعليًا.
**التحقق:** 19/77 أثرًا → 77/77 · دالتان تقولان نفس `SIE_RUNTIME_VERSION` · اختبار يفشل الآن ويسمّي 20 معرّفًا.
**الرجوع:** كل بند علم مستقل. لا تغيير schema عدا `sie_refund_message` (إضافة بحتة).

> **شرط الخروج — لا تبدأ Phase 1 قبله:** كل دور مدفوع له أثر مع `reason`، ونسخة محرك واحدة، وCI يعمل.
> **السبب:** كل رقم في `SIE-ARCHITECTURE.md` مأخوذ من 19 أثرًا وقياسات على Node لا Deno. بدون 0.2 نحن نحسّن بلا قياس.

### Phase 1 — أين تذهب الثواني الثلاث

**الهدف:** تحويل «>97% غير مفسَّر» من `[فرضية]` إلى رقم.
**تحل:** W-04 · O-01 · O-02 · O-05

| # | العمل |
|---|---|
| 1.1 | تجزئة `processing_time_ms` → `cdnMs` · `dbMs` · `cpuMs` · `waitMs` — **أولًا، قبل أي تحسين** |
| 1.2 | حزم الـJSON مع الدالة (يلغي تبعية jsDelivr ويحل T-availability) |
| 1.3 | حذف `sessions.saveState()` — كتابة مكررة مؤكَّدة |
| 1.4 | مؤشر تقدمي في `normalize()` (يحل الثلاثة معًا) |
| 1.5 | `recallPreviousSession` بـ`select bot_state->'sie'` |
| 1.6 | توازي المستقل (`Promise.all`) |

**المخاطر:** 1.2 يغيّر آلية النشر — يُختبر على دالة واحدة أولًا. 1.3 يبدو حذفًا خطرًا: التحقق أن `persist_bot_turn` يكتب `bot_state` **مؤكَّد من تعريف الـRPC الحي**.
**التحقق:** p95 دافئ < 600 ms · تجزئة تقول أين الباقي.
**الرجوع:** 1.2 عودة إلى الـSHA المثبت.

### Phase 2 — فك الارتباط بحجم الكتالوج

**الهدف:** أن تصير تكلفة الدور دالة في الرسالة لا في الكتالوج.
**تحل:** W-01 · W-03 · B-08 · W-08

| # | العمل |
|---|---|
| 2.1 | `ScenarioIndex` — فهرس معكوس، `WeakMap` على مصفوفة الكتالوج |
| 2.2 | `retrieve()` قبل التسجيل — المرشحون فقط |
| 2.3 | الحالة = سجل أدلة + حالة قرار، `schemaVersion`، والفرضيات تُشتق |
| 2.4 | رقم إصدار للجلسة + `where version = ?` |
| 2.5 | `ScoringStrategy` و`EvidenceModel` كواجهتين — **بلا تغيير سلوك** |
| 2.6 | توحيد عتبة التنشيط (تمريرها إلى `updateHypotheses`) |
| 2.7 | فك الـbridge إلى `TurnRunner` + `ConversationPolicy` + `SettingsToPolicy` |

**المخاطر:** 2.3 **يغيّر شكل `bot_state`** — أخطر بند في الخطة. التخفيف: `schemaVersion` + قراءة الشكلين + تجاهل القديم بعد نافذة. لا migration للبيانات: الجلسات القديمة تنتهي طبيعيًا.
`providerOver()` في الـresolver يُنتج **مصفوفة جديدة كل دور** عند تشغيل الـoverlay — سيُفوِّت الـWeakMap. يُحل في 2.1 أو يبقى الـoverlay مطفأً.
**التحقق:** حالة < 10 KB عند 650 · النواة ×31 أسرع · **726 اختبارًا تمر بلا تعديل** (2.5 و2.6 لا تغيّران سلوكًا).
**الرجوع:** 2.1–2.2 خلف علم `SIE_RETRIEVAL`. 2.3 خلف `schemaVersion`.

### Phase 3 — الذكاء، مقاسًا

**الهدف:** خفض الغموض والتعادل. **شرط الدخول: Phase 8.1 (shadow-run موصول).**
**تحل:** W-02 · B-04 · T1 · T2 · D-04

| # | العمل |
|---|---|
| 3.1 | خصوصية التوكن (IDF) داخل `ScoringStrategy` |
| 3.2 | عقوبة ما لا يُفسَّر من الرسالة |
| 3.3 | كاسر تعادل دلالي بدل `localeCompare` |
| 3.4 | `contradicts` من النص + تحلل زمني في `EvidenceModel` — يحل T1 و T2 |
| 3.5 | سقف أثر للدور الواحد |
| 3.6 | توصيل `typo-tolerance` بوزن أقل |
| 3.7 | رفع تغطية الأسئلة التمييزية 12% → 35%+ للمجموعات المتنازعة |

**المخاطر:** 3.1–3.3 **تغيّر أي سيناريو يفوز**. لا تُنشر بلا shadow-run على الـ650. القياس يُظهر تحسنًا في الغموض (12.2%→5.8%) مع **تراجع طفيف في الفوز بالمسبار الذاتي (95.4%→94.8%)** — لأن عقوبة التغطية تفضّل السيناريو الذي يفسّر الرسالة **كلها**. يُحسم بمراجعة بشرية لعينة، لا بالرقم وحده.
**التحقق:** على الـ650: الغموض < 8% · التعادل التام < 2% · الفوز ≥ 94%.
**الرجوع:** `ScoringStrategy` قابلة للتبديل بسطر.

### Phase 4 — البنية التي تفتح السقف

**الهدف:** التوقيع المفصّل. **هذا هو التغيير المعماري الكبير الذي طلبتَ تفضيله على الترقيع.**

| # | العمل |
|---|---|
| 4.1 | `domain` · `topic` · `tags[]` في الـschema — **إضافة، لا كسر** (`category` يبقى للتذاكر، يُشتق من `domain`) |
| 4.2 | ترحيل الـ650: مُشتق ميكانيكيًا من بادئة الـid (57 بادئة → ~12 مجالًا) + مراجعة بشرية مرة واحدة |
| 4.3 | استنتاج الفاصل في `Interpretation` |
| 4.4 | `scenario-lint` — L1 subset · L2 تعادل تام · L3 حد أدنى للتخصيص · L4 مجموعات مغلقة · **L5 لا ترفع الإضافة غموض الكتالوج** · L6 `maxSpanWords ≤ 8` |
| 4.5 | تحليل التغطية من `rawText` غير المطابَق |

**L5 هو الفحص المفصلي:** الوحيد القادر على رفض سيناريو سليم الشكل لأنه **يضر بالكتالوج ككل** — وهو بالضبط ما حدث بين 325 و650 بلا أي إشارة.
**المخاطر:** 4.2 يلمس 650 صفًا — مُشتق آليًا ومراجَع، لا مكتوب يدويًا.
**التحقق:** على الـ650 بعد الترحيل: غموض ≈ 0% · تعادل ≈ 0%.

### Phase 5–7 — Composition · Memory · Actions

| المرحلة | البنود |
|---|---|
| **5 Composition** | مزوّد معرفة لحظي حقيقي (يجعل `knowledge_priority` يعني شيئًا) · `knowledgeSource` لأكثر من 4 سيناريوهات · دمج `rescueWithArticle` في `composeAnswerDecision` · استعادة النصوص الأربعة إلى `templates` |
| **6 Memory** | مفاتيح ذاكرة من **مجموعة مغلقة** (يحل T3) · سياسة `INSERT/UPDATE` لـ`sie_customer_memory` (معطّل على الويب اليوم) · نقل `answeredScenarioIds` عبر الجلسات · سياسة احتفاظ لـ`bot_state` و`rawText` |
| **7 Actions** | مسار مرفقات حقيقي — **شرط لإبقاء `ASK_FOR_*` أصلًا** (29% من الحالات تطلب ما لا يُقرأ) · `pendingAction` عام بدل `pendingTicketConfirmation` · حد تذاكر لكل عميل (T10) |

### Phase 8 — الرصد والتحقق

| # | العمل |
|---|---|
| 8.1 | **توصيل `shadow-run` و`comparison` و`replay`** — مكتوبة ومختبرة وتنتظر أسطرًا قليلة. **شرط دخول Phase 3** |
| 8.2 | توصيل `publish-gate` (T5) + دور `catalog_publisher` منفصل عن `support` (T6) |
| 8.3 | metrics: معدل الفشل · توزيع القرارات · p95 · معدل الغموض |
| 8.4 | تنبيه على: `overlayStatus=unavailable` · فشل تحميل الكتالوج · قفزة `handled:false` |

### Phase 9 — تعدد القنوات

مدخل واحد يوجّه بالمسار · مفاتيح لغة لـ`CHANNEL_REPLIES` (عربية فقط اليوم) · dedupe مشترك · `sie_rate_limit_hit` على مسار القنوات · قدرات القناة معلنة.

### Phase 10 — الذكاء المتقدم

**القاعدة: لا مكوّن احتمالي قبل أن يعمل shadow-run.**

| الموضع | الحكم | السبب |
|---|---|---|
| الترتيب والقرار | **حتمي — لا LLM** | قرار يفتح تذكرة يجب أن يُفسَّر بـ`evaluatedRules`. والمشكلة معادلة ناقصة لا نموذج ناقص |
| توليد النص النهائي | **حتمي — قوالب** | 343 حلًا مراجَعًا بالعامية المصرية. إعادة صياغتها تضيف خطر تلفيق بلا قيمة |
| **استنتاج الفاصل (domain/topic)** | **LLM — أقوى موضع** | **مقيس: مفسِّر بدقة 80% يعطي 91.4% فوزًا مقابل 82.5% لمطابقة توكنات مثالية مسطّحة** |
| **الاسترجاع عند 10,000+** | **embeddings — للترشيح فقط** | المطابقة المعجمية تفوّت ما لم يُسرَد. الـembedding يرشّح، والقواعد تحكم |
| توسيع المعجم | **LLM خارج وقت التشغيل** | اقتراح أنماط من `rawText` غير المطابَق → مراجعة بشرية → JSON. صفر خطر وقت تشغيل |
| تأليف السيناريوهات | **LLM خارج وقت التشغيل** | مسودة توقيع + كشف تشابه. أكبر رافعة إنتاجية للـ350 القادمة |
| ما لا يغطيه الكتالوج | **LLM مقيَّدًا داخل `FALLBACK` وحده** | اليوم نص عام. حدوده: لا حلول ملفقة، وتصعيد عند الشك |
| رسالة طويلة متعددة المشاكل | **LLM كـsplitter قبل البايبلاين** | المحرك يفترض مشكلة واحدة لكل دور. مرشّح لا حاكم |

**حد الثقة مع الـLLM:** مخرَجه يدخل كـ`Signal[]` من **مفردات مغلقة** — لا نصًا حرًا، ولا اسم سيناريو، ولا فعلًا. فإن هُوجم أو هلوس، أسوأ ما ينتج **ترشيح خاطئ** تردّه القواعد، لا **قرار خاطئ**.

### Phase 11 — التعلّم

طبقة **مستقلة تمامًا عن مسار الطلب**:

```text
آثار + تصحيحات المراجعين
        ▼
اكتشاف الأنماط (غير متزامن)  ← لا يكتب شيئًا يقرأه المحرك
        ▼
اقتراح: نمط معجم · توقيع · سيناريو جديد
        ▼
مراجعة بشرية  ──► shadow-run على الـ650  ──► publish-gate  ──► إصدار مرقَّم
```

كل تغيير **observable · auditable · reversible · versioned · validated**.
**لا self-modification.** المحرك لا يكتب في كتالوجه أبدًا. T12 يُمنع بفصل قناة الاقتراح عن قناة الاعتماد.

---

## 4. الترتيب والاعتماديات

```text
Phase 0 ─┬─► Phase 1 ─► Phase 2 ─┬─► Phase 4 ─► 5,6,7,9
         │                        │
         └─► Phase 8.1 ──────────►┴─► Phase 3 ─► Phase 10 ─► Phase 11
             (shadow-run)             (المقياس)
```

**ابدأ بـ:** 0.1 · 0.2 · 0.3 — النسخة الواحدة، والأثر لكل دور، والكوتة العادلة.
**لا تلمس الآن:** R0…R9 · Action Layer · أمن القناة · `resolver` · `catalog-integrity` · `settings-schema` — كلها تعمل ويجب أن تبقى.
**لا تضف سيناريو واحدًا** قبل Phase 4.4 (`scenario-lint`): كل 100 سيناريو ترفع الغموض بلا إشارة.

---

## 5. معايير النجاح

| المحور | اليوم | بعد Phase 4 | كيف يُثبت |
|---|---|---|---|
| زمن النواة @1,000 | 0.75 ms | **0.008 ms** | benchmark مقارن ✅ مقيس |
| زمن النواة @100,000 | 292 ms | **0.36 ms** | benchmark مقارن ✅ مقيس |
| حالة الجلسة @650 | 206 KB | **549 B** | قياس مباشر ✅ مقيس |
| حالة الجلسة @100,000 | 31 MB | **549 B** | مستقلة بالبناء ✅ مقيس |
| غموض @650 | 12.2% | **0.0%** | نفس المسابر ✅ مقيس |
| تعادل تام @650 | 16.8% | **0.0%** | نفس المسابر ✅ مقيس |
| تغطية الأثر | 25% | 100% | عدّ من القاعدة |
| p95 دافئ | ~2,900 ms | < 600 ms | من الأثر المجزّأ |

**اختبارات تهديد مطلوبة (غير موجودة اليوم):** حقن الأدلة عبر الأدوار · تلويث الذاكرة · تسميم الحالة · تلاعب الترتيب · التعادل التام · كتالوج ضخم · حالة جلسة ضخمة · سيناريو مشوَّه · محادثة عدائية.

**ولا يُحكم بـbenchmark واحد:** كل مقارنة على 100 · 650 · 1,000 · 10,000 — و100,000 للأداء والتخزين فقط، لأن جودة القرار فوق ~3,600 **غير قابلة للقياس بكتالوج اصطناعي** (سقف التمايز نفسه). هذا حدّ صدق في المنهج، لا كسل في القياس.
